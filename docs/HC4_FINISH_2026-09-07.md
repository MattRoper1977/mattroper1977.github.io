# HC4 finish ledger — 2026-09-07

Order `mbm-two-domain-healthcheck-2026-09-07-HC4`. Status at the end of this
order: **HC4_PARTIAL**. Every section below ends with its token; every count
names the script that produced it; every served claim names a run and a witness
or says "merged, not live".

Anchors at start (R1): Site `0113130e` · Lessons `e5129ad8` · Apps `162e65b3`
· Games `15e2c9cf` — all four confirmed by clone before the first edit. Open-PR
touch sets were censused by script into `open_pr_census.json` (20 PRs, every
file list paginated and count-verified). Re-anchored after every merge; the
closing SHAs are in the readback.

Rules applied: no owned source edited (Site `#25`, `#109`, `#216`, `#288`,
`#291`; Lessons `#118`, `#116`, `#35`, `#43`, `#45`; Apps `#2`, `#4` untouched);
Lessons `#93` closed as superseded with the reason on the PR (R2); Science
releases, Matt-marked PRs, released pins Site `a30088ea` / Lessons `13845784`,
staged branch `pin-release/lessons` `3fdefbe0` and Lessons `#340`'s merged fix
all preserved (R5). One exception, stated up front and detailed in §5.2: the
Games pin workflow itself re-staged `pin-release/site` from `1570c2af` to
`ad7bd0c7` when Site main moved; no hand touched that branch. Both domains stay proxy-403 from the session (R4): nothing
below is called "live" without a publication run at the released SHA.

## §1 SAVES — adapters beyond Glitch, old-www stranding

**1.2 Keys by instrument.** `scratchpad/keys/harness.cjs` wrapped
`Storage.prototype.setItem/getItem/removeItem`, `indexedDB.open` and
`IDBObjectStore.put/add` before any page script on the locally built Play
tree (Site `0113130e` / Lessons `e5129ad8`), loaded each candidate, drove one
save through the game's own path and logged every write in order. Positive
control: Glitch Clash wrote `glitchclash_save` (400 B) and its export blob was
byte-identical to the stored value. Inventory: `scratchpad/keys/inventory.json`
(one record per route: boot writes, driver, keys written with byte sizes,
import path/format, export function, `importEqualsStorage`, first boot write).
Typical pupil save sizes are these harness sizes, not estimates.

| game | keys written (bytes) | import consumes | wrap byte-equal to export? | first boot write | adapter |
|---|---|---|---|---|---|
| Glitch Clash (control) | `glitchclash_save` 400 | raw campaign JSON | yes (identical) | 40.6 ms probe only | served (HC3) |
| Emberwild | `mbm_emberwild_SLOT_1` 2176 (+`_TEMP`) | checksummed `.emberwild` envelope | no — envelope ≠ storage (timestamp/party/commitRevision/checksum) | 1606 ms (splash key; save key never at boot) | report(format) |
| Global Games V4 (`/olympics/`) | `mbm_global_games_world_stage_v4` 1596, `mbm_sports_passport_v4` 731 | V4 collection bundle | no — bundle wraps and re-sanitises | 55.7 ms (passport), save re-persisted at 57 ms | report(format, boot-order) |
| Apex Kick | `apexkick.aaa.v4` 1053 | own envelope `{game,build,exportedAt,save,passport}` (3166 B) | no | 97.6 ms profile; save re-persisted ~83–110 ms | report(format, boot-order) |
| Lumina Haven | `mbm_lumina_haven_state_v1` 1507, `…_v6_profile` 327 | raw state JSON (pretty) | structurally (whitespace only) | 86.2 ms (profile key) | report(boot-order) |
| Aurora Links V4 | `mbm_aurora_links_aaa_v4` 1261, `mbm_sports_passport_v4` 1070 | V4 collection bundle | no — section is `cleanSave(profile)`, differs in `best` | 83.1 ms (passport), save at ~53 ms | report(format, boot-order) |
| Touchline | IndexedDB `career_slots/autosave` 118 926, `slot_1` 118 924 (no localStorage) | crc32 career envelope (266 KB export) | no — record vs envelope fields differ | 2079 ms (splash only) | report(format: IndexedDB, not Storage; >32 KB → file only) |
| Titan Forge | six `mbm_titanforge_*` keys 102–497 | `TFS1.<Z|R base64url(deflate JSON)>.<fnv8>` code | yes (keys byte-identical inside the code; `ascension_v1` never written) | 177 ms (`mbm_titanforge_save_v1`) | report(boot-order; deflate wrap would exceed the shared stub) |
| Biopunk Hive | `mbm_biopunkhive_save` 567 | Base64(UTF-8) of the stored JSON | yes (decoded base64 === stored) | none — zero boot writes | candidate (receiver not landed — see below) |
| Town Life | `mbm_town_life_v10` 4326 | raw state JSON + `exportedAt`,`product` | no (two extra fields) | 53.5 ms probe; save only on `saveNow` | owned (Site #216) |

**1.3 / 1.4 / 1.5.** Under the order's rules only Biopunk Hive qualifies for a
new transport (byte-equal wrap that is the game's own base64 line, an import UI,
and zero boot writes). Its receiver/sender was specified (fragment `mbm_import`
+ `mbm_sum` SHA-256, receiver prefills the game's own Validate Import and
leaves Overwrite to the pupil, planted one-byte corruption rejected by digest)
but the implementation agent was cut off by the session limit before any file
was written; no branch exists. **No adapter beyond Glitch is served by this
order.** Adapter column (1.8): served 1 (Glitch, HC3) · report 8 · owned 1.

**1.6** Town Life stays owned: Site #216 is agent-authored, not superseded (the
inert-subtree guard is not on main) and not green (two red checks on a base 40
commits behind), so R2 neither merges nor closes it.

**1.7 Old www origin.** `docs/HC4_WWW_EXPOSURE.md` + `tools/hc4/www_exposure.py`
(merged in Site #299): across the four working trees, files carrying
`www.madebymatt.uk` = 7 (all Site domain-split host allow-lists), routes ever
linked as www = 1 (the bare origin), printed/worksheet/QR/hub sources = 0;
positive control `www.madebymatt-play.uk` = 25 files / 44 occurrences on the
same scope. History is depth-1 in three clones, stated as "unknown", not
faked. No recovery attempted; M1 is Matt-only.

Token: **PARTIAL** — inventory by instrument done (10/10), sizes measured,
www exposure written; adapters served 1 / report 8 / owned 1; Biopunk
receiver not landed.

## §2 HUMANITIES DECK RESIDUE

R6 first: `_sownb/VB_STATE.json` is not closed (`resume_easter_a3.status` and
`resume_aav_night.status` both `RUNNING`) and `claude/*` branches moved inside
the last six hours at the anchor. HC4 therefore edited no deck.
`_sownb/vb/HC4_DECK_TARGETS.json` (Lessons branch `claude/hc4-two-domain-healthcheck-e9cqlp`,
commits `956068d9`/`b195cde8`) carries the 16 routes from
`docs/HC3_DECK_RESIDUE_2026-09-07.json` with every hash re-checked at hand-off
(`_sownb/vb/tools/hc4_rehash_deck_targets.py`: 16/16 MATCH; self-test real 0 →
planted 1 → restored 0), the 12 + 4 batch split, the proposed objective edits
and rules 2.1–2.8 as targets. Counts by script: 16 decks, 48 duplicate id
names, 96 occurrences. Kept under `_sownb/` because the education publisher
admits the Lessons tree by exact path and a new root file would be UNREVIEWED.

The branch is open as Lessons draft PR #369 — a hand-off record, not a merge
candidate for an agent on green.

Token: **HANDED(VB stream, Lessons single writer)**.

## §3 ZOOM

**3.1 Instrument.** `tools/hc4/pinch_instrument.cjs` (Site #300). Positive control
(plain page) and negative control (`user-scalable=no` + `touch-action:none`)
both pass in one run for two variants; a discriminating control (five scratch
pages, one blocker each) showed the CDP `synthesizePinchGesture` default source
zooms through `touch-action:none` alone — a trackpad-like pinch — so the
two-finger `Input.dispatchTouchEvent` variant (positive 1 → 5, negative 1 → 1,
honours `touch-action`, `user-scalable` and non-passive `preventDefault`
individually) is the instrument. Site #291 stays HELD; its route verdicts remain
MEASUREMENT INVALID; nothing derived from its runs is counted.

**3.2 Measurement.** 26 routes derived from the Apps #37–#40, Site #278 and
Lessons #350 file lists (`tools/hc4/zoom_population.json`). Headless, on
locally served copies of the trees, informational: before 19/26 zoom, after the
declaration fix 26/26. Not a phone; not "pinch proved".

**3.3 Declaration fix.** `tools/hc4/zoom_declaration.cjs` (static viewport meta +
computed `touch-action` chain, three-run control every run). Before: 13/26 pass.
After one CSS token per declaration (`touch-action:none → pinch-zoom`) on
twelve routes: **25/26 pass**; the remaining FAIL is `/neonmeridian/`, owned by
Site #216. Claim wording: "zoom not blocked by declaration". Branches
`claude/hc4-zoom-declaration`: Site `593fb08e` + `52c0253` + merge of main + `69dd94f` (PR #300; the neonsync and neonbreach audited-source identity pins re-derived for the one-token change), Apps `333aed4` (PR #51), Lessons `28dad241` (PR #366). Play revision
registry and education transition pairs prove real/planted/restored.
Merged: Site #300 → `baf52157` (Site's own publication runs `34110118875` domain
split and `34110118975` education, both success); Apps #51 → `585d50c7` (Apps
publication `34112663203` success); Lessons #366 → `968c9434` (Lessons
publication red at that SHA for the §4 registry reason, cleared by Site #302 and
the third publisher pin — see readback). Served on Play: **no** — the games are
a pinned copy (R8); the staged `pin-release/site` `ad7bd0c7` (Site `50877370`)
moves exactly four payloads' bytes (`/fracture/`, `/neonsync/`, `/neonbreach/`,
`/apexrally/`) and waits on M3. One visible consequence, recorded rather than
hidden: Site's `Neon Breach live verification` push run `34110118838` at
`baf52157` reads PENDING (served 124 874 B = pin `a30088ea` bytes, tree
124 880 B) and that workflow exits 78 on PENDING, so it shows red until the Play
pin moves. Content is not wrong; the pin is behind.

**3.4** `docs/HC4_PINCH_PHONECHECK.md` — 26 routes, one line each, for the human
phone check.

Token: **PARTIAL** — instrument valid, 25/26 declaration-clean, merged on all
three mains, education copies published; Play copies not served until the pin
moves (M3); phone check written, not run.

## §4 DONATION REFERENCES — 5 links / 7 occurrences

**4.1 R2 on the three owners** (`open_pr_census.json`):
- Site #25 — title `HOLD: … do not merge until activation is settled`, Matt-marked,
  touches `index.html` (root, 5 occurrences / 3 links). Untouched → M2.
- Lessons #93 — codex-authored draft, 15 commits on a base 30 days behind,
  `mergeable_state: dirty`, carries its own one-shot workflow. Superseded on the
  evidence (poster now `/assets/video/poster-art.webp` on main; Orbital rebuilt
  through #360; the pH accessor guards a jsdom-only `innerText.trim()`). **Closed
  with the reason on the PR.** `/Lessons/` `index.html` is free.
- Apps #2 — body opens `Do not merge — Matt merges`, Matt-marked, touches
  `index.html` (1 occurrence). Untouched → M2.

**4.2** Pupil surface freed: Lessons `index.html:389` Ko-fi button removed on
branch `claude/hc4-lessons-kofi` (PR #367; `e60d913f` + `4134401e`, no other copy
changed). Two prerequisites were met first: Lessons #365 (merged `1ab4e1ac`) pins the
publisher release whose census excuses a held route at zero, and the cross-estate
gate's reviewed Lessons catalogue digest was re-pinned with the estate's own
`tools/catalogue/pin_catalogue_contract.py` (reviewedFiles 421, originalRowsPreserved
734, appendedShelfRows 3) in both gate copies — Apps #52 carries the mirror.
Merged: Lessons #367 → `fbd3377b`, Apps #52 → `9df2d879`. *Second incident,
recorded:* the Lessons publication at `fbd3377b` (run `34111899163`) then
stopped on `CHANGED education-lessons/index.html` — the exact-byte registry
still admitted the hub with its button. Site #302 fixes the registry as a
**transition pair** (the mechanism from §3.3): Site's own workflows build the
education trees from Lessons `419d44f0` (button present, digest `21cdc649…`)
while the Lessons caller builds from Lessons main (button gone, `110a8a0c…`),
and one digest cannot admit both — the first attempt (`bee1599e`, single new
digest) went red on Site's own build for exactly that reason. Proof: both
pairings built in CI order (`build_publications` → `build_education` →
admission) exit 0 locally with the two digests observed, Ko-fi count 1 and 0.
Site #302 merged → `4281da4c`; Site's own publication at that SHA green
(education `34114509747`, domain split `34114509748`). Third publisher pin to
`4281da4c`: Apps #54 merged → `09e01a86`, Lessons #370 merged → `dd484670`.
The Lessons publication at `dd484670`: **green** (run `34115311456`, the first green since #364). The two Lessons
main workflows that had gone red on the same root (`fieldops-p2-and-sweep`'s
"Merged is not served" needs a successful exact-source publication;
`watch-main` aggregates it) cleared: the serve proof was re-run by genuine `workflow_dispatch` once the
publication existed (run `34116118490`, green); `watch-main` follows on its
next trigger.
Adult hub links: Science
Teaching_Packs and Humanities David_Cover keep one configured footer each to
`https://ko-fi.com/madebymattuk`, the URL in `domain-split/education_support.py`
(`KOFI`); the existing `domain-split-verify` step "The configured Ko-fi URL
resolves to the account's own page" is the CI fetch — unchanged.

**4.3 Gate.** `domain-split/check_education_support.py` on the rebuilt tree
(Site #299): `kofi_on_pupil_routes` 0 of 461 counted pupil routes; the held
record excuses `/` 5, `/Lessons/` 1, `/Matt-s-Apps-/` 1 at their recorded
counts or at zero (resolved); `planted_footer_control` red then green. A
literal estate-wide zero is still false while Site #25 and Apps #2 hold their
pages.

M2 (Matt): `mattroper1977.github.io/index.html` line 10 (`mf-band-link`) and
the four other Ko-fi occurrences on the root page under #25;
`Matt-s-Apps-/index.html:233` (`mbm-kofi` button) under #2.

Token: **PARTIAL** — one route freed, merged and (once the third publisher pin
lands) published; two routes Matt-owned (M2); gate green on main with the held
allowance named.

## §5 WEEKLY / PIN AUTOMATION

**5.1 M3** unchanged and still required: Games → Settings → Actions → General →
Workflow permissions → tick "Allow GitHub Actions to create and approve pull
requests". No repository permission was changed by this order.

**5.2** Games #72 merged (`9dd8b449`): on PR-denied the workflow (a) leaves the
staged branch untouched — it reads the staged `play-publication.json` first and
re-stages only when the source HEAD moved; `pin-release/site` `1570c2af` and
`pin-release/lessons` `3fdefbe0` were not overwritten — (b) opens or updates ONE
issue "Pin bump staged — awaiting M3" (branch, SHAs, gate results; `issues:
write` added to the workflow's own permissions block), (c) exits green with a
job summary. Any other creation error still fails. Controls in
`tools/pin_release_evidence.test.cjs`: denial classified, unrelated error stays
red, malformed staged manifest reads not-current, exact-title single issue,
two matches stop. First run under the new code: the 10:17 UTC cron did **not**
fire (GitHub's scheduler; stated, not faked), so the workflow was run by genuine
`workflow_dispatch` — run `34111060809`, Games `4e318bdf`. `site` job: every
gate green on the proposed tree (publication contract, save boundaries, shelf
contracts, browser acceptance, perf 69 routes 0 regressed); PR creation
denied; ONE tracking issue opened — Games #74 "Pin bump staged — awaiting M3";
job exit **green**. Two facts against the draft of this section: (a) Site main
had moved (`a30088ea → 50877370`), so the workflow's pre-existing
"source moved" path **re-staged** `pin-release/site` from `1570c2af` to
`ad7bd0c7` — #72 only guards the "already carries this HEAD" case, and the
order's never-overwrite reading was not met for that branch. `1570c2af` still
resolves through the API; creating a preserving ref from this session was
refused by the proxy (no write to `git/refs`), so it is an item for Matt or
for nobody: `ad7bd0c7` supersedes it with green gates. `pin-release/lessons`
`3fdefbe0` untouched. (b) The `lessons` job went **red**, not on PR-denied but
on the build: "Unreviewed or ambiguous source revision:
Lessons/Games/Glitch_Clash.html" — the workflow builds Lessons main against the
*released* Site pin `a30088ea`, which predates Site #301's registration of the
HUD revision. The Lessons pin therefore cannot move until the Site pin has
(site first, then lessons); a real gate, correctly red.

**5.3 Site #288** — hold reason from the PR itself: "full green is not
available under these measured access constraints" (its own weekly job reports
the estate's reds). Reds it named, now: MTR and published-live re-run on
current main by genuine `workflow_dispatch` — **both green**
(`34101499078`, `34101501750`, Site `0113130e`); J4 dispatched — green
(`34103276852`, Lessons `e5129ad8`), no longer stale; pin-release — fixed by
#72, awaiting its first scheduled run; apexpool-sports-verify is declared
retired in `data/retired-checks.json`. #288's own weekly check was re-dispatched
on its branch after the pin run (`34113929866`, `b7e0aaf2`): its controls all
pass (planted RED / STALE / retired / own-row / stale-pin), and its report reads
**NOT CLEAR — 6 red, 1 stale, 0 stale pins**. The six, every one accounted
for: Site `domain-split-verify` (the first #302 commit, cleared by `47c0bd3b`);
Site `neonbreach-live-verify` (PENDING until the Play pin moves, §3); Lessons
`education-pages` (the §4 registry, cleared by Site #302 + the third pin);
Lessons `fieldops-p2-and-sweep` ("Merged is not served" needs a successful
source-bound publication for main — same root); Lessons `watch-main` (never
succeeded; it aggregates the two above); Games `pin-release` (the lessons job's
real build gate, §5.2). The stale one is Site
`made-by-matt-showcase-capture` (last success 30.1 days, currently green).
Its verdict is by design one signal per red, so the PR stays red while any of
those is; R2 outcome: report, not merged — the hold reason still holds, and
now names exactly which reds and why. Nothing was edited on its branch.

**5.4** MTR is `workflow_dispatch`-only (no path, no cron); published-live has
`push` paths (`ouroboros/`, `novasiege/`, `games/index.html`, its own YAML) and
cron `17 6 * * 1` (Monday 06:17 UTC). Today's 06:17 schedule did not fire
(GitHub's scheduler; last scheduled run is older). Both were run genuinely by
dispatch as above; no run was faked.

**5.5** Pin age per source repo in the weekly report: carried by #288's
`tools/estate_pin_report.py` (per-commit `ageDays`, per-source `oldest_game_age`,
report-only); the re-dispatched run above prints it — Site pin `a30088ea` 3
game-touching commits behind main, oldest unreleased game change 0.067 days;
Lessons pin `13845784` 5 behind, oldest 0.089 days — so the column works on the
branch. It lands on main when #288 merges. The file is owned by #288, so nothing
was added on main.

Token: **PARTIAL** — 5.2 merged, proven by test and by one genuine run (site
green with the tracking issue, lessons red on a real build gate, staged site
branch re-staged); 5.3 reported; 5.4 genuine runs; 5.5 blocked on #288's
ownership.

## §6 GLITCH HUD + HISTORICAL PERFORMANCE

**6.1** Measured on the served Play copy at 390 px with `getBoundingClientRect`:
HUD back circle ∩ Home heading = 912 px² (∩ header row 1444 px²), on the Home,
Campaign and Team screens, after a real fragment import and on plain navigation.
Bisect: the same measurement at every Glitch commit since the handoff and back
to `4ddb2849` (1 September) — `4ddb2849 4391c399 e8b73767 bc29591e b3ff7d70
c5415da8 bc6a1af8 95146202 e80ac328` — reads 912 px² at every one: the overlap
predates the receiver; origin is the HUD injection meeting `.screen`'s 14 px
top padding. Fix: one rule, phone widths, only when the HUD is present
(`body:has(#mbmhud-back,#mbmexit-back) .screen{padding-top:…}`). Proof real 0
→ planted 912/1444 → restored 0; desktop 0 before and after; ten Glitch suites
pass. Lessons #364 merged (`76e1f466`); rollback `e5129ad8`. Merged, **not on
Play** until a Lessons pin moves.

*Incident recorded honestly:* the publisher's Play revision registry pins every
reviewed revision of Glitch, so after #364 the Lessons main publication went
red (runs 34104585926 at `76e1f466` and 34108589120 at `1ab4e1ac`,
"Unreviewed or ambiguous source revision"). The previously published Lessons
tree stayed served. Fix: Site PR #301 (**merged**, Site main `50877370`) registers
`glitch-hud-clear-390-2026-09-07` as a reviewed revision without making it
current (the handoff tooling's reviewed receiver stays the one Play serves until
the pin moves); revision controls real/planted/restored pass. Lessons and Apps
re-pin the publisher caller to `50877370`: Lessons #368 → `fbd3377b`, Apps #53 →
`08f7ed11`, both merged. The Lessons publication stayed red for the separate §4
registry reason until Site #302 and the third pin (see readback).

**6.2** Games #72: `docs/hc3-recovery/history/HC3_HISTORICAL_PAYLOAD_BYTES_2026-09-07.json`
frozen with `SHA256SUMS` (`0110e55d…eaf95`), 69 rows, 20 344 993 B, 16 above
500 000 B; README declares per-route gzip, the 18 quiet identities, the 19-slow
table and the timing harness UNRECOVERABLE; HC1 numbers with no artefact are
"unattested".

**6.3** Games #73 merged (`4e318bdf`): `tools/hc4_perf_baseline.mjs` (reports
only; self-test 180 → 720 → 180 idle rAF) and
`docs/HC4_PERF_BASELINE_2026-09-07.json` on the tree Play serves (pins Site
`a30088ea` / Lessons `13845784`), 69 routes × 3 runs, all measured, labelled
"current-main baseline (not historical)". No threshold enforced.

Token: **CLOSED** for 6.2 and 6.3; 6.1 **PARTIAL** (merged, not served).

## §7 SHARED STATS + SCOPE STATEMENT

**7.1** Site #299 (`47811e56`): `/stats/on-this-device/` generated on Play as
well as education, paths bound per origin; gate
`domain-split/check_device_stats_record.cjs` (route 200, one record written the
way the page's helper writes it and read back, every subresource 200) real →
planted → restored on both origins, wired into `domain-split-verify`. Local run
PASS/FAIL/PASS twice; CI on #299 green. Merged; Site's own publication of
`47811e56` ran green (education `34106281230`, domain split `34106281225`,
deployment provenance `34106778703`), so both device-stats routes are on the
published education tree.

**7.2** Shared collection stays OFF. M4 (Matt-only), the three activation steps
from `domain-split/analytics-backend/README.md`: (1) finish the hosted
acceptance on the Supabase project (`fhulisooqhbyldphmnca`): enable Supabase
Cron, apply `setup_retention.sql`, verify one successful `mbm-usage-expiry` run;
(2) run `activate_after_acceptance.sql` (sets `sources.enabled` for education
and play — no event is created by activation); (3) set `features.stats.enabled`
and `remote` to true in `site.json` and re-publish (the Play copy the builder
writes follows). No shim, no placeholder counters were added.

**7.3** Verbatim: "Science completion covers Weeks 3–7 only. The
curriculum-through-Easter request is NOT finished by this order; it belongs to
the VB stream (VB-EASTER-A3, Lessons single writer)." This order built no
lessons.

**7.4** Every moved-page stub now carries exactly one link (Site #299): the
template, the stub verifier (planted four-link control) and the separation
check all count total anchors; 79 stub digests replaced in the exact-byte
admission registry with a proposal diff showing no other path moving.

Token: **CLOSED** (7.2 M4 listed; 7.1/7.4 merged and published by Site's own
green publication run; "served" in the R4 sense — see the witness paragraph
under §8 — is not claimed).

## Mains at the end of the order

Site `4281da4c` (plus this ledger's own docs-only merge) · Lessons `dd484670`
· Apps `09e01a86` · Games `4e318bdf`. Open HC4 PRs: none, except Lessons draft
#369 (the §2 hand-off record). Nothing in this order touched a Matt-marked
PR, a Science release, a staged pin branch by hand, or a released pin.

## §8 LEDGER + READBACK

**R4 witness, measured not assumed.** `tools/hc_readback_counts.py --refresh`
was run from the session at the closing mains (snapshot
`docs/HC4_GITHUB_SNAPSHOT_2026-09-07.json`, output
`docs/HC4_READBACK_COUNTS_2026-09-07.json`, both replayable with
`--snapshot`): `publication_deployed` **true for all four** repositories
(Site `4281da4c` run `34114509747`, Lessons `dd484670` run `34115311456`, Apps
`09e01a86` run `34115178162`, Games `4e318bdf` run `34106691504`);
`publication_and_live_proved` **0 of 4**, reason on every row "No
publication-bound byte witness supplied". The witness envelope needs the
Lessons `serve-proof` artifact bytes, and the artifact blob host
(`*.blob.core.windows.net`) answers 000 through the session proxy, so no
envelope could be built and `--self-test` refuses to run without a real proved
publication — which is the instrument working as designed. Consequently every
HC4 change on the education domain is **published** (a green publication run
at the released SHA) and none is called **live**; the Play domain games are
not even published until the pin moves. The Lessons `fieldops-p2-and-sweep`
serve proof at `dd484670` (run `34115310773`, GitHub's own fetch of the 44
FieldOps subjects against the exact publication) is **green** on run
`34116118490` (`workflow_dispatch`, an event the envelope decoder accepts; the
push run `34115310773` was INCONCLUSIVE because the publication finished after
its 240 s retrieval bound). Its `serve-proof` artifact is `10016325373`; wrapping it
into a byte-evidence envelope is the one step this session could not perform.
A person or a CI job with artifact access can: fetch run, job, artifact and
archive for `10016325373`, write them as a schema-1 envelope beside
`docs/HC3_BYTE_WITNESS_2026-09-07.json`, and re-run
`tools/hc_readback_counts.py --snapshot docs/HC4_GITHUB_SNAPSHOT_2026-09-07.json
--byte-evidence <envelope> --self-test`.

This file, docs-only PR on Site main, merged on green. Readback follows in the
closing message.

Token: **CLOSED**.
