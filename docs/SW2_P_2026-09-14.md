# SW2 P — Play header token parity

Matt approved P with H/U/A on 14 September 2026, including necessary small gate changes and checked publication. This is stacked after H/U; it changes only the Play header and its acceptance assertions.

The existing approved mark, Made by Matt wordmark, bold Play, search icon and native Menu already meet the requested layout. The composed Play tree already contains the reviewed token file, but its header did not load or consume it. This change adds one origin-local token stylesheet link and scopes dark token aliases to the existing masthead. All shelf content, lanes, card counts, game payloads, scripts, previews and save behavior are preserved.

The full UX2 C4 gate remains required. Its four existing first-load viewport cases additionally verify the token HTTP response and complete source SHA-256, a single local link, dark header theme, decoded mark, bold Play label, token ink adoption, 44px controls and the search focus action. The existing zero-Education-origin-load assertion and all 69 initial game-surface checks remain. This is not a full gameplay-completion claim.

Fresh output comparison against the unchanged H/U Games tree: 479 files; only six shelf HTML aliases and assets/play/play.css change. All game payloads and other 472 files are byte-identical. No Games configuration is manually rewritten. After Site merge, use the established Games Pin release owner, re-run its source preservation, save, C4, performance and required-branch checks, then verify the exact source pin and live composed header. Until that completes P is PARTIAL, not SW2_P_OK.

Observed existing owner blocker before P: Games run 34781808396 passed source/gameplay/performance and the contract/aggregate checks, but GitHub rejected the merge with “2 of 2 required status checks are expected.” The active Games ruleset has no bypass permission. Preserve the rule; investigate through the existing owner path and report any unresolved publication block.
