# HC5 ledger — 2026-09-07

Order `mbm-finish-2026-09-07-HC5` (top/bottom sentinels `mbm-finish-2026-09-07-HC5-TOP/BOTTOM`).
Supersedes HC4 for its residue; HC4's ledger (Site `5cfe7982`, PR #303) is the state of record
this order started from. Exit token at the end of this file. Every § ends with its token; every
count names the script that produced it; "published" means a green publication run at the
released SHA and "served" means bytes fetched from the origin and matched — they are never
conflated here.

**Reading of the bracketed rulings.** The order's D2/D3/D4/D6/D7 lines each carry an
alternative in brackets (`[KOFI=keep]`, `[PR216=hold]`, `[PR288=hold]`, `[BIOPUNK=skip]`,
`[WITNESS=skip]`). This order executed the rulings **as written** (remove / rebase / rebase /
build-last-or-hand / build the witness) and treated the brackets as the swap Matt could have
made but did not. If the brackets were meant as the live values, every change below under
D2, D3, D4 and D7 is a single revert each, and the PR bodies say so.

## §0 STATE (zero writes)

0.1 Mains at start (prediction met exactly): Site `5cfe7982` · Lessons `dd484670` · Apps
`09e01a86` · Games `4e318bdf`. Pages run ids at those SHAs are the rollback points (Site
education publication `34114509747`, Lessons `34115311456`, Apps `34115178162`, Games
`34106691504` — the readback instrument's own snapshot in HC4).

0.2 VB_STATE (`_sownb/VB_STATE.json`): `resume_easter_a3` RUNNING, `resume_aav_night` RUNNING
(prediction met). Re-read before §2.2: RUNNING. Re-read before §4: RUNNING (both streams). No Lessons deck
was edited by this order; new paths, hub HTML and the resources.json append were the only
Lessons writes, each pushed after fetch → rebase.

0.3 Open-PR inventory (`scratchpad/hc5/open_prs.json`, 20 PRs at start): Site #291 (HELD),
#288 (HELD), #216, #109 (Matt: do not merge), #106, #91, #25 (Matt: HOLD); Lessons #369
(HC4 hand-off), #118 (Matt: PARKED), #116 (Matt: REFERENCE DIFF), #94, #45 (HELD), #43
(HELD), #35 (HELD), #31, #30, #26 ("Do not merge yet"), #17; Apps #4 (Matt: yours to
merge), #2 (Matt: Matt merges). Nothing the record did not know. Games: issue #74 open.

0.4 M3: the Actions permission endpoint is not reachable through the session proxy, so the
probe is the evidence trail instead — Games issue #74 still open, no PR ever created by the
pin workflow, the 10:22 dispatch's site job again denied. **Not ticked.** §3.3 is HANDED in one
line; nothing waited on it.

0.5 Zips present, sha256 as predicted: BUILD `52dd218671b5c770…`, GROW `f05e74b9cecc4679…`,
LAUNCH `78e3fa23c1c952f5…`; census 96 = 32/32/32 by `unzip -Z1`; `unzip -t` clean on all
three. The two uploaded copies of `Humanities_Autumn1_Review_Overview.pdf` are byte-identical
(`eb2df97c…`) and, per D1, not shipped.

0.6 Discovery surfaces re-classified as the order records them (Lessons resources.json
AUTHORED · Site data/source-manifests/lessons-resources.json DERIVED · Site
tools/build_mbm_search_index.py generator · sitemap.xml AUTHORED). Teaching_Packs entries in
each: **0/0/0/0** (grep). The 15 classroom routes named in the three Start Here guides exist
on Lessons main **15/15** (file-exists by script); serve-200 is UNMEASURED from the session
(both origins proxy-403) and is covered by Lane W's instrument instead.

Token: **CLOSED**.

## §1 HUMANITIES PACKS

1.1 Fixes — `scratchpad/hc5/tools/fix_packs.py`, XML level, idempotent (second run over its
own output: 0 files changed). Log per file with sha256 before/after:
`_packs/HUMANITIES_A1_FIXLOG.md` (Lessons, unpublished path).

| defect | predicted | measured | proof |
|---|---|---|---|
| H1 Adolphus 21 → 24 (BT 26/1237) | 8 DOCX, 0 PPTX | **8 DOCX + 1 PPTX** (the GROW W3 deck repeats the answer line on all 12 notes slides): 20 occurrences | crop of rows 505–506 (`_packs/HUMANITIES_A1_H1_BT26-1237_rows_505-506.png`): AMOS Kenneth 21 Welder / ADOLPHUS Rubert **24** Carpenter |
| H2 Start Here wording | 3 DOCX | 3 DOCX, 12 edits (subtitle, publication-status paragraph deleted, sources paragraph replaced, "Published 7 September 2026" after CHECKED) | zero-count over 142 shipped files 0/0/0/0; red-proved on the originals 6/6/6/6 files (3 DOCX + 3 PDF) |
| H3 orphan media | image2.png ×15, logo copy ×33 | **48 parts in 33 DOCX** (image1.jpg ×33, image2.png ×15), 48 relationships dropped | 22/22 orphan-only DOCX render pixel-identical before/after under the same LibreOffice; the H1/H2 files differ only on their edited lines |
| H4 notes line | × 36 | **× 3** — the line sits once per deck, on notes slide 4, in every deck; removed from BUILD W3/W6/W7 only | the only text token that differs in those decks' XML is that line |
| H5 thumbnail swap | optional | **skipped** by choice | — |

PDFs regenerated only for edited sources (37: 33 DOCX + 4 PPTX) with LibreOffice 24.2
headless (Writer/Impress had to be installed in the session; the shipped PDFs were LibreOffice
26.8 renders). Render gate (`gate_packs.py`): page counts equal the originals for all 37 (decks
12; Pupil 3/4; Teacher 3/4; Start Here 4/5/5), ink on every page, "24" in the Adolphus row of
all 9 H1 files (the deck read through its notes XML — notes are not part of a slide render).
Untouched files keep their original bytes (11 PPTX, 11 deck PDFs).

Verified and not touched: image credits (NPG 6856 PD-Art; Geograph CC BY-SA 2.0 with photographer
and date; TNA BT 26/1237), all dates, timetable slots, 40-minute timings.

Instruments wrong before right (mandatory): (1) the first PPTX pass re-zipped untouched decks
and changed their bytes — 48 "changed", corrected to 37; (2) the first H2 count ran over the
fixed-source folder that still held the original PDFs and read 6/3/3/3 "after" — moved to the
built tree and made to fail on fewer than 90 files scanned; (3) the deck H1 check first read the
slide render, which never contains notes; (4) the anchor-reachability probe waited 50 ms against
a smooth-scroll page and read 1/15 — at 700 ms it reads 15/15, and the Science index reads 15/15
as the control.

1.2 Tree — `build_teaching_packs.py`: `Humanities_Teesside/Teaching_Packs/{index.html,
packs.css, DOWNLOADS_MANIFEST.json, SHA256SUMS.txt, BUILD/, GROW/, LAUNCH/}`, one `Week_n/`
convention (not Science's three). Per pathway: Start Here DOCX+PDF, Week_3…Week_7 (zip names
verbatim, post-fix), `downloads/`: Complete_Pack, PPTX/DOCX/PDF collections, W3–W7 Teaching_Pack
×5 and `<Slug>_Lesson_Pack` ×5. 141 files + SHA256SUMS; 42 zips, every one `unzip -t` clean and
byte-identical across rebuilds (fixed timestamps); sizes computed, in the Science index's
decimal units (377/377 of the Science labels measured decimal). Tree size 121 MB, of which the
42 zips are 94 MB; the Lessons repository was 845 MB before this order.

1.3 Index — structural clone of the Science pack index (same CSS links, header, breadcrumb
"Learning home / Humanities by term", theme bootstrap, skip link), hero "Humanities teaching
packs", three pathway sections with the required links, sticky Week 3–7 anchors, per-week six
file links with type and size, matching classroom lesson links (§0.6), footer "Weeks 1–2 are
baseline/retrieval; Week 8 (w/c 19 Oct) is not in this pack.", credit line per pathway to the
Start Here guide. Rendered (`render_index.cjs`, Chromium) at 390 px and 1280 px: 0 px horizontal
overflow, 19/19 anchors resolve, 15/15 week anchors reachable by click, 0 failed requests, no
review/draft/held wording.

1.4 Two taps — Humanities hub gains the Science hub's catalogue-links entry in the same position
(first link); the root hub's label reads "BUILD, GROW & LAUNCH Science teaching packs" (LAUNCH
was already present) and gains a sibling Humanities card. Hard-coded confirmed by two instruments
(grep of the HTML; rendered catalogue-links text absent from resources.json).

1.5 PR A — Lessons #371 (`386bacfb` + `dafd491b`), 13 checks green, merged → **`5c2af15f`**;
Apps #55 gate mirror merged → `af74fb60`. PR B — Lessons #373 (`75a6355a`): six rows (3
Humanities, 3 Science), prior 737 rows byte-identical, shelf rows 3 → 9, both gate copies
re-pinned; red-proved by one injected fake-subject row (red on the row contract and the
manifest pin; restored green); Apps #57 mirror. Merge state: Lessons #373 merged → **`423067f9`** (13 checks green); Apps #57 merged → `5faeaeb9`.

The publication chain after PR A (the same route HC4 took for the hub, now with one more explicit
registry state): Site #305 (merged → `ae6fa26b`) admits the 142 new paths as **ARRIVING** —
one reviewed digest plus the literal, absent-tolerated only because Site's own workflows still
build the education trees from Lessons `419d44f0`, any other bytes still CHANGED, six controls —
and pairs the two hubs and `data/usage-registry.json`; Site #308 pairs the Creator Hub and PR B's
five derived outputs. **Site #308 is the blocker of the whole chain.** Every check that builds its branch is green, but the repository's required status check "Fetch the live estate and compare to raw-at-SHA" (`agx1-live-verify.yml`) checks out and builds **main** by design ("the deployed tree") — so on any PR it can only report main's stale Neon Meridian evidence, which is the red #308 repairs. Merge refused by the repository rule (405). No workflow edit was made outside §3.5 and no check was edited to pass. **M5 (one tap): merge Site #308 with bypass, or drop that check from the required set until main is green.** Until then the fourth publisher pin (Lessons + Apps → post-#308 Site main) cannot be cut, and the Lessons publication (red since #371 on UNREVIEWED, run `34124898801` at `423067f9`) and the Apps publication (red since #56 on CHANGED index.html, run `34124902556` at `5faeaeb9`) stay red. Everything for the pin is prepared (`scratchpad/hc5/pinbump3.sh`); a self check-in is armed for 13:53 UTC to cut it the moment #308 lands.

1.6 Served proof — **SERVED=UNPROVEN.** Both origins are proxy-403 from the session; the CI instrument that would close this (Lane W, §3.5) exists on main and its first run read the Lessons publication INCONCLUSIVE because no exact-source publication for Lessons main has succeeded since #371 — which is the #308 blocker above. One link for Matt: https://madebymatt.uk/Lessons/Humanities_Teesside/Teaching_Packs/ (expect 404 until the pin lands; then the index and one PPTX are the taps).

Token: **PARTIAL — fixed, landed on Lessons main (PR A + PR B) and discoverable on main; not yet published or served, blocked on Site #308 (M5)**.

## §2 LESSONS TIDY

2.1 D13 table (agent-held → merged on green or closed with a one-line reason; Matt-marked →
untouched, listed):

| PR | marker | disposition |
|---|---|---|
| #369 HC4 §2 hand-off | none (mine) | merged as data → `0e0cd743` (D10) |
| #94 repair trigger | none | closed superseded (temporary trigger on a non-main base) |
| #31 session-close records publisher | none | closed superseded (Aug 3 helper; docs since rewritten) |
| #30 Art Teesside validation jobs | none | closed superseded (dirty; 37 commits behind the later Art work) |
| #17 wire_lessons drift assertion | none | closed superseded (carries 349 vendored node_modules files; re-cut the one file if wanted) |
| #26 Art Teesside visual upgrade | "Do not merge yet" | untouched, listed |
| #118, #116, #45, #43, #35 | PARKED / REFERENCE DIFF / HELD | untouched, listed |
| #371, #372, #373 | none (this order) | see §1, §3.1 |

#340 resolution written (D10): `_sownb/hold-history.json` (no hold-history.json existed in any
repository — HC4 looked — so it is created under the unpublished `_sownb/`): merged by Matt
2026-09-06 as `f50f0b2f`, content on main. Lessons branch `claude/hc5-lessons-tidy`
(`96340976`) — Lessons #374 opened, merge on green.

2.2 The 16 alternative decks: VB_STATE RUNNING at this moment → **HANDED** with the same
`_sownb/vb/HC4_DECK_TARGETS.json` (now on main via #369) and one line: re-paste when VB pauses.

2.3 Science Teaching_Packs index: every one of its 377 size labels re-measured against the
files — 377/377 correct in decimal units, labels consistent; no moved files, no edit needed. Hub
cards for both subjects: painting on the local render of the two hubs (Chromium, `hub_instrument.cjs`: both catalogue-links entries visible, width > 0); the public render waits on the same #308/pin chain.

2.4 HC3 classroom residue not covered above (worksheet-only print, phone-hidden TA baseline):
measured only as far as the record allows without a deck edit — both are deck-level defects under
D11's rules and stay with the VB stream alongside 2.2. Owners: VB (Lessons single writer).

Token: **PARTIAL** (2.2 HANDED; the rest CLOSED).

## §3 HC4 RESIDUE LANES

3.1 Lane K — Ko-fi per D2. Measured, not carried: the served `/` carried five Ko-fi references,
**one** in the generated root hub (`data/audience-homepages.json` → `index.html`, not the hand
edit the order located at line 10) and **four** in the education home template
(`domain-split/preview-template.html`: a donation button, two support cards, two footer links
across its views). Site #304 removes all of them at their sources and regenerates; census on
the rebuilt tree: `kofi_on_pupil_routes` 0/461, `/` **0** (held allowance resolved), 155
remaining Ko-fi hrefs on adult pages, `kofi_hrefs_off_url` **0** (every one equals the URL in
`education_support.py`), planted footer control red then green. Apps #56 (merged → `f4f69b31`)
removes the Creator Hub's "Buy me a coffee" line; the cross-estate gate's authored-wording
comparison admits exactly that reviewed wording by digest (planted extra edit → red; removed →
green); Lessons #372 (merged → `2b38ac02`) mirrors the gate. Instrument wrong before right: the
first root-hub edit was by hand on a generated file and the renderer's own control caught it.
Site #304 (five commits, conflict with #305's registry resolved by keeping main's registry plus the three D2 digests) is green on every check that builds its branch and red on the same main-building required check as #308 — M5 unblocks it; Site #25 is closed under D2 once #304 merges (left open until then). Apps #2 closed superseded with the reason on the PR.

3.2 Lane T — #216 per D3: branch brought up to date with main (server-side merge), prediction
written on the PR before the one re-run ("both reds green"); result: "Generator controls, SS1–SS8,
and Site routes" stayed red — 11 of 18 splash targets on main were never regenerated with the
branch's `visible()` guard ("18 applied; 0 written; 11 divergent"). Closed as superseded. Re-cut:
the neonmeridian pinch declaration only (Site #307, merged → `a34eef4c`, declaration gate PASS
with its three-run control → 26/26). The inert-subtree guard cannot land for Town Life alone —
the generator check treats every splash target as derived — so it is listed for Matt (generator
+ 18 regenerated targets = a Play-served byte change for 18 games). **Consequence recorded
honestly:** #307's one token made Neon Meridian's Play evidence stale and Site's own publication
went red at `a34eef4c`; Site #308 refreshes the evidence (HC4 #300 did this with its tokens;
#307 did not). #308 is blocked by the required check that builds main (M5).

#288 per D4: brought up to date with main; prediction written before the run; its report stayed
NOT CLEAR and named a PR-branch red (maker-splash on #216's re-run) as an estate red — its own
instrument fault, fixed inside the PR (runs filtered to `branch=main`, self-test passing). After
that fix its reds are: Site `neonbreach-live-verify` (PENDING until the Play pin moves),
Games `pin-release` (lessons job's real build gate), Lessons `education-pages` and `watch-main`
and Apps `verify-lundyloop-professional-os` (all the in-flight HC5 publication reds, main's),
plus the report's own row. Marked ready; merge under D4's exempt-with-cause precedent: **merged → `58e08509`** (its live-estate run had completed green before main broke); HC4 §5.5's pin-age rows now land on main.

3.3 Lane P — HANDED: M3 is not ticked (0.4), so the site pin, the lessons pin the run after,
the Neon Breach re-verify and #74's self-close all wait on one owner tap. D5 recorded: the
`pin-release/site` re-stage `1570c2af → ad7bd0c7` is designed behaviour; no preserving ref;
moved on.

3.4 Lane Z — neonmeridian via #307 (declaration 26/26 on main); Play copies via Lane P (M3);
the phone check stays human (D8, `docs/HC4_PINCH_PHONECHECK.md`).

3.5 Lane W — serve witness per D7 (Site #306 merged → `95bac35c`; follow-up #309): one additive
workflow `.github/workflows/serve-witness.yml` + `tools/hc5/serve_witness.py`; fetches the
review artifact of each repository's successful source-bound publication for its current main
(the FieldOps serve proof's selector, precedent artifact `10016325373`) and byte-compares 19
served subjects across both origins (the Humanities index, three Start Here PDFs, one PPTX/DOCX/
PDF and the three Complete_Pack zips among them). Self-test: real WITNESSED → planted byte RED →
foreign origin RED → 404 INCONCLUSIVE → unreachable INCONCLUSIVE → restored WITNESSED → absent
subject INCONCLUSIVE. First dispatch (run `34123836892`): **0/4** — site, lessons, apps
INCONCLUSIVE (their exact-source publications were red at that moment, the reds §1.5 and 3.1
repair) and games RED with no per-subject detail in the log (fixed in #309). #309 carries the per-subject logging and the report-only artifact; it is blocked by the same required check (M5). The next dispatch after the pin lands gives the measured n/4.

3.6 Lane B — Biopunk adapter per D6: **HANDED**, nothing half-landed. Spec pointer: HC4 ledger
§1.3–1.5 (`docs/HC4_FINISH_2026-09-07.md` on Site main: transport-only, keys by instrumented
setItem log, boot-order gate, planted-corruption control; Biopunk's own base64 export line as
the wrap). Session budget went to §1 and the repairs above.

3.7 Untouched by design, restated verbatim for Matt:
- M1: hosting-level exception serving a ≤2 KB page on www.madebymatt.uk for one path (the 301
  fires before any page runs); if refused, stranding accepted and the stub copy must say so
  (copy = Matt).
- M4: Supabase (1) finish hosted acceptance: enable Cron, apply setup_retention.sql, verify one
  mbm-usage-expiry run (2) run activate_after_acceptance.sql (3) set site.json features.stats
  enabled/remote true and republish.

Token: **PARTIAL — K/T/W landed on main or ready and blocked on M5; P HANDED (M3); Z closed on main; B HANDED**.

## Mains at the end of the order

Site `58e08509` (plus this ledger once M5 lets docs PRs merge) · Lessons `423067f9` · Apps
`5faeaeb9` · Games `4e318bdf`. Open HC5 PRs: Site #304, #308, #309 (all green on every check
that builds their branch, all held by the main-building required check — M5), Lessons #374
(tidy, merge on green). Matt-marked PRs untouched: Site #109, #25 (closes with #304), #291;
Lessons #118, #116, #45, #43, #35, #26; Apps #4.

## §4 CLOSE

This file, docs-only PR on Site main, merged on green. Readback follows in the closing message.

Exit token: **HC5_PARTIAL**
