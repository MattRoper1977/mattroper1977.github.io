# Homepage mock-up correction — 14 September 2026

Matt compared the live homepage with the approved phone mock-up and reported missing icons and details. The live implementation used Unicode text symbols, rendered all 13 catalogue groups as large homepage tiles, and used the earlier H3 no-preview fallback. The pathway strip, pack panel, recent additions and teacher band existed but were pushed far down the phone page.

This correction restores the four compact featured subjects using inline SVG line icons; the full record-driven catalogue remains one tap away through View all. Audience subject browsing retains every actual group and now uses vector icons. The homepage has a search icon with an accessible button name, a person icon in the compact teacher band, a full-width three-pathway strip, tighter mobile spacing and compact recent rows. The original silver M, shared navigation, approved pathway colours, real catalogue titles, dates, descriptions and URLs remain authoritative over the generated mock-up.

Prepared preview images are first-page JPEG renders of the actual published teaching PDFs, produced using Poppler at 540px with JPEG quality 80. `homepage-previews.json` records the catalogue identity, PDF path, page, role and source SHA256. At build time, `preview_data` admits an image only if the matching record lists that PDF and its current source bytes match. Changed or missing PDFs produce no image. No render dependency or workflow change is introduced. The runtime only uses admitted previews for the currently selected real pack group or recent row. Rows without a verified preview have a book icon. None of the illustrative lesson titles or pictures in the mock-up is invented as catalogue content.

The six changed publication paths are the homepage and its main alias, teacher/pupil pages, the front-door CSS and the existing recent-catalogue script. The admitted current rollback for each path remains alongside the measured candidate. Apps, Lessons files, games, shared header/footer and other audience bodies are not edited by this change. Play release #369 and the immutable carriers are left to their existing owner.

Checks retain the complete publication/navigation suite, current catalogue comparison, safe pupil search, touch targets, contrast and negative controls. The focused checks now require the four compact SVG subjects and prove each preview's PDF record and bytes. The planted extra-subject control still verifies the full audience catalogue and the homepage's View all route; a new real/stale/restored PDF-source control proves obsolete previews are suppressed. Full-page captures at 390, 900 and 1280px are written to `audit-output/education-navigation/homepage-repair/` for review.

Status: correction prepared; no merge, deployment or completion claim until CI, screenshot review and live checks pass.
