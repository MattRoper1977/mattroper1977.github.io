# SW2 H/U — homepage and audience front doors

Matt explicitly approved H, U, A and P on 14 September 2026, including small necessary gate changes, checked publication and live verification, without another Ready prompt.

H and U share the Education front-door generator. The homepage uses the approved lesson search, subject tiles, three noninteractive pathways, real pack text, the first three recent catalogue rows, audience shortcuts and a quiet teacher band. Teachers get the four teaching shortcuts and verbatim record safety note; pupils get subject tiles and their existing pupil-safe search. Empty subject groups are omitted. The six other audience outputs remain byte-identical.

H3 AUTO-DECISION: no previews. Lessons uses a pinned reusable publication caller with no existing PDF render hook. Adding a new render dependency and mutating its source catalogue is outside a clean hook; no placeholder or stock image is substituted. The pack panel derives the most recent begun half-term carrying packs from the served calendar; when only future blocks exist it selects the first available block. Format and pathway chips come from actual files and records. There is no separate About page; the existing commission route remains the Meet Matt destination.

Only four Education HTML outputs, the existing recent-catalogue script and one new scoped stylesheet change. All 3,200 pinned Lessons files, 106 Apps files and 479 composed Play files match the K publication byte for byte. No lesson content or app payload changes.

The original UX2 B5 gate remains, including reachability, money, third-party loads, consent choices, catalogue comparison, claims and negative controls. Copy, three-row and no-preview expectations follow the approved SW2 supersession. A new negative control removes the teacher safety title. New checks cover real pack formats/pathways and pupil search normal/results/empty states at 390, 900 and 1280 pixels. Existing chrome, reading variants, publication and live provenance gates remain enabled.

Admission changes are limited to the six output paths. Each retains the currently served K rollback; four obsolete pre-K digests are retired to preserve the two-digest limit. No workflow or pinned input changes, held branch rewrite or runtime bypass is included.

Base/rollback: Site b5f69d74e77b1955077874fc7b21a03b07b031fa.
