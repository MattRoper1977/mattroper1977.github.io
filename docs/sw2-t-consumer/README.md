# SW2 Part T — lessons publisher candidate

REVIEW ONLY. This is an immutable consumer-builder candidate, not a branch to merge into Site main. Its parent is 11fa3b83fbb7ddf42d2631dda3e2942028ef911e.

Only education-lessons navigation surfaces use the reviewed current shared chrome. Other publication outputs, companion source pins, Science/Computing contracts, the resource-size derivation and existing release checks remain unchanged. The shared component and templates are copied at exact reviewed bytes from the Part T candidate; the adapter retains the carrier's other renderer. Root assets are supplied by the Site release, which must land first.

The actual built output reaches the unchanged all-file admission boundary. The separate admission-plan.json specifies the exact proposal; the active registry remains untouched. Do not select this builder for production until the named digest retirements are approved and the candidate passes the normal release checks. The original carrier and its registry remain available for rollback.
