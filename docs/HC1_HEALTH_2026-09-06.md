# HC1 HEALTH — 2026-09-06 (Site repo, madebymatt.uk education root)

Token `mbm-two-domain-healthcheck-2026-09-06-HC1` · verdict **HC1_PARTIAL**. The
estate-wide ledger and map live in the Lessons repo:
`Lessons/docs/HC1_HEALTH_2026-09-06.md`, `Lessons/docs/ESTATE_MAP_2026-09-06.md`.
This file carries only what is this repo's to act on. Measured at main `d9115711`.

## Served proof at HEAD
`Deployment provenance` run 34044012989: origin serving `d911571`, 9 witnesses
byte-identical (`index.html`, `main/`, `resources/`, `tools/`, `for/teachers/`,
`for/pupils/`, `data/domain-catalogue.json`, `resource-collections.json`,
`resource-discovery.json`). `Published Education completion` run 34044013536 green.
Education publication run 34043927215 deployed (job 101515683894).

## Findings owned here
1. **Verifiers still pointing game routes at `madebymatt.uk`** (they now fetch 1.1 KB
   stubs): `published-live-verify.yml` (schedule Monday 06:17 UTC — reds from 2026-09-07
   on `/ouroboros/ /novasiege/`), `driving-games-live-verify.yml`, `neonbreach-live-verify.yml`,
   `olympics-live-verify.yml`, `titan-crown-live-verify.yml`, `mtr-live-gate.yml`,
   `post-merge-production-verify.yml` (+ `tools/verify_production_after_merge.mjs` BASE),
   `tools/verify_v4_games_deployment.mjs` (eight canonical literals), `apexpool-home-verify.yml`,
   `mbm-audience-discovery-closeout.yml`. `agx1-live-verify.yml` already reads
   `madebymatt-play.uk/games.json`. Repair: origin table → `https://www.madebymatt-play.uk`
   and compare against the host-rewritten publication (`domain-split/build_publications.py`
   output), never raw source. Not done: R3 respected by merging nothing that depends on them.
2. **`estate-check-health` red on every run since 2026-08-26** (schedule Monday 07:00 UTC).
   Its report step exits 1 by design when gaps exist; the 08-31 report says Lessons and
   Apps have 0 required checks and Games merged over a red 9 times. A finding for Matt,
   not a broken instrument. Rulesets measured today: Site `21475918` and Games `21475919`
   active; Lessons and Apps none.
3. `domain-split/build_education.py`: (a) `moved_page()` stubs lack
   `<link rel="canonical" href="<play url>">`; (b) `tools/artsaward/SLOTS.json` is not
   published, so ten Art decks' staff slot panel now falls back to manual upload
   (pre-split it was served); add it to `public_file` beside `tools/index.html`, then bump
   `builder_ref` in Lessons and Apps `education-pages.yml`.
4. `domain-split/education_support.py` `LESSON_ADULT` puts the Ko-fi support footer on
   `/Lessons/`, `/Lessons/Science_Teesside/`, `/Lessons/Humanities_Teesside/` — hubs
   pupils reach from `/for/pupils/`. Matt's ruling.
5. `usage_discovery.py` → `/stats/on-this-device/index.html` references `../styles.css`,
   `../favicon.svg`, `../assets/mbm-features.*`, `../assets/brand/micro_mark.svg` — resolve
   to `/stats/…` (404). Root-anchor them.
6. `/evidence-binder/moderator-pro/` CSP blocks the injected `/assets/education-support.css`.
7. Education root tree: 187 files, 81 html (34 full, 42 game stubs, 5 `/next/` continue
   pages); 0 third-party subresources; Ko-fi links on 26 adult pages; 24 pages link to
   play (40 links, labelled); search index 677 entries, 0 game/play; sitemap 424 locs.

## Not measured from this venue
http→https redirect, `www.madebymatt.uk`, external link targets (proxy 403).

Rollback for anything above: main `d9115711a989da54229441c3ae64549aa1d5f0eb`.
