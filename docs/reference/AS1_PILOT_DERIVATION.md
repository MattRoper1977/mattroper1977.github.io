AS1-R pilot derivation — current canonical Games manifest, 2026-09-11.
Ownership means exact game-file membership in an open PR changed-file list. Declared PR scope is not ownership.
The 62 canonical records are the sole pilot universe. Seven publication-only activities are excluded from this universe.
Original-source checks execute reset/state functions in Node VM with enumerated presentation stubs. They are not browser, served-page, rendering or playability proofs.
A KEEP can be limited to an existing explicit seed/configuration. No new seed interface or game hook was added.
No game file was edited. Ouroboros is excluded because its file is changed by open Site PR91.

01 DROP /cyberpulse/ — player exposes x/y and visual tilt, but no per-frame world heading field. Renderer snapshot carries that same player object; tilt is banking, not heading.
02 DROP /crownbadge/ — The authoritative state contains officers, sectors, incidents and resources. It has no per-frame moving player position-plus-heading object.
03 DROP /houseolympiad/ — Player denotes a profile on a championship dashboard, not a geometric in-frame avatar.
04 DROP /titanforge/ — PhaseController.reset chooses gameplay braceSeed and braceSign using unseeded Math.random. animate consumes both to drive the isometric balance drift, so these are simulation inputs, not visual particles. No seed argument is accepted by reset.
05 DROP /emberwild/ — game.player.render.x/z/y and facing are together, but resetForNewJourney seeds both world and simulation RNG from Date.now with no supplied seed argument. Start cannot be reproduced across independently begun journeys as implemented.
06 KEEP /novasiege/ — player.x/y/aimAngle are one per-frame object. beginRun preserves the actual selectedSeed for retry and recreates player, RNG and wave plan; the VM executes those original functions and confirms two resets and 24 frame samples match by value. Missing-heading and wrong-seed controls fire. This keeps one selected game configuration repeatable; it does not claim that selecting Daily again on another date yields the same seed.
07 DROP /ouroboros/ — Owned by open changed-file list: Site #91. Owned route may be measured only and cannot be edited for this pilot.
08 DROP /olympics/ — Global Games has no horizontal player-heading field in its per-frame athlete state. Sprint, hurdles and swimming expose distance/speed, long jump uses take-off angle, and javelin rot follows the projectile. SkiJumpEvent does expose x/y/pitch in flight, but pitch is aerodynamic up/down body tilt rather than a horizontal heading; during inrun, rendered position comes from s and rendered rotation is a fixed 33 degrees. Curling stone.rot is accumulated stone spin, not travel heading. These fields are reported by their implemented meaning, not silently relabelled as heading for admission.
09 DROP /fracture/ — Zone geometry and enemy positions are seeded, and player transform resets from zone.spawn. However, each enemy's initial attackCooldown and patrolAngle use Math.random, so the actual starting simulation is not deterministic.
10 KEEP /neonturf/ — Original Match constructor and resetPositions restore gameplay geometry; original updateCar updates position and heading in one Car. Exact seed input and fixed foundry arena replay by value; both firing controls fired.
11 DROP /echovault/ — Player starts at x=0,z=14,yaw=0, but reset calls seedSectorEnemies, which draws guard angles and extra spawn positions from unseeded Math.random/rand.
12 DROP /relicforge/ — Normal runs use unseeded Math.random. Daily runs seed all starter-part quality, vault generation and initial enemy state correctly, but startRun accepts only options.mode and invokes dailySeedDescriptor() without a date/seed argument. The helper optional date parameter is not exposed by the start path. Daily runs match within a calendar day, not for the unchanged configuration across dates; no Date mock or new seed hook was used to manufacture admission.
13 DROP /offbrand/ — S.player supplies x/y/path/speed/state but no current heading. Characters travel path points; inventing an angle field would be an implementation change rather than deriving an existing pilot.
14 DROP /Lessons/Games/Axiom_Shift.html — Per-frame position and heading are not exposed together: runner horizontal display position is rx=W*.28; state contains altitude and song clock. Ring gravDir is the direction of gravity. A synthesized heading would be a new derivation.
15 KEEP /Lessons/Games/Charcoal.html — CH.S.player x/y/_fx/_fy; heading = atan2(_fy,_fx), with default (0,1). CH.newRound accepts seed, map, role, round; seeded entry proven; normal Play default is Date.now Existing explicit seeded entry reproduces full state and a 60-tick trace twice. Original-source VM control changes initializer _fx:0 to _fx:1 and reddens reset comparison; restored original greens. No claim normal Play already chooses a repeatable seed. See charcoal_vm.json.
16 DROP /Lessons/Games/Hold_the_Mark.html — Tower defence state represents structures and enemy waves; S.dir fields lastEval/surges/eased describe the director, not player heading.
17 DROP /Lessons/Games/Glitch_Clash.html — Card battle has no moving player pose. The x/y/vx state belongs to draggable card UI and cannot serve as player telemetry.
18 DROP /trailrunner/ — window.__TR.player yields Ee mesh with local x/y and z fixed at 2. Actual track progress Er and track tangent ba(Er) live outside that object; visible orientation is applied to separate ft mesh. Ee alone does not expose the player's track position and heading.
19 DROP /Lessons/Games/voxelcraft.html — Player position and view heading are separate object3Ds. Even seeded Daily World spawn does not reset camera orientation, so re-entering it does not establish a deterministic player start. Ordinary and arcade worlds use unseeded randomness.
20 DROP /Lessons/Games/Vortex.html — Authoritative world position is wt/At/xt scalar state; rendered Tt carries position and rotation but z is fixed rs. The per-frame object passed to __VORTEX_V6_FRAME__ has x/y/d and no heading. No single object proven to hold full track position plus heading. Same seed regenerates the course, but per-object world-pose predicate fails.
21 KEEP /Lessons/Games/Globe_Snake (1).html — We.P contains spherical player position and We.T its tangent heading vector. Mh initializes both; Sh updates both on the same entity.. Original Fr reset, Jg/t0 orb schedule, Mh player and relayPlace target run twice in the same VM at explicit seed 12345; equal world and 60-step trace. We.P contains spherical player position and We.T its tangent heading vector. Mh initializes both; Sh updates both on the same entity. Original Fr reset, Jg/t0 orb schedule, Mh player and relayPlace target run twice in the same VM at explicit seed 12345; equal world and 60-step trace.
22 DROP /Lessons/Games/Neon_Snake_Overdrive.html — Pose is split between snake head and dir; first food varies on reset, so world start is not deterministic.
23 DROP /Lessons/Games/Neon_Siege.html — Tower defence: player places stationary towers and enemies traverse path. A tower orientation or enemy heading is not a per-frame player pose.
24 DROP /Lessons/Games/Neon_Garden.html — Garden placement and growth state; plant positions are not a moving player object.
25 DROP /Lessons/Games/Orbital.html — DROP: authoritative We has x/y/vx/vy; aim vector qn is separate. The bt mesh is a possible same-object pose, but Ch changes bt.rotation.y while nl does not reset that rotation. orbitCancelAim clears es; u0 only rewrites heading when We.flying or es is true. Thus the existing reset path does not establish a fixed mesh heading. Th/restart/resethole call nl; nl resets We position and zero velocity without restoring bt heading. A seed fixes course data but does not fix this retained start orientation. Source reading establishes the missing reset; no runtime failure is claimed.
26 KEEP /Lessons/Games/Grid_Chase.html — player.c/r/tc/tr/t provide current interpolated position through original entXY(player); player.dir provides heading. Original stepGrid updates that entity.. genMaze(seed+level*101), seeded gameRandom and newRun/newLevel/spawnPlayer run twice in one VM at explicit seed 12345, with identical maze, player, hunters, relays and 60-step trace. player.c/r/tc/tr/t provide current interpolated position through original entXY(player); player.dir provides heading. Original stepGrid updates that entity. genMaze(seed+level*101), seeded gameRandom and newRun/newLevel/spawnPlayer run twice in one VM at explicit seed 12345, with identical maze, player, hunters, relays and 60-step trace.
27 DROP /Lessons/Games/Prism.html — The c/r/dir object describes a ray in the beam-tracing queue, not a player. Player action places mirrors/splitters.
28 DROP /Lessons/Games/Grapple.html — DROP: the required source-defined heading is not present. cg creates Ce with x/y/vx/vy, hook and rope state; vg places ln from Ce and changes its scale but never sets player facing. Velocity is not an existing heading field, and selecting atan2(vy,vx) would introduce a new convention, including a stationary convention. Seeded lg and Gc/cg establish a source reset path. No heading convention is initialized or reset there, so the complete position-plus-heading criterion is not established. No runtime failure is claimed.
29 DROP /Lessons/Games/Marble.html — DROP: ag creates Ye with position, velocity, falling and respawn fields, but no heading. lg constructs a sphere; pg accumulates its orb.rotation.x/z from velocity as rolling orientation, while parent zt carries position. The source does not define that accumulated roll as travel/facing heading or define a forward axis. zc reinitializes Ye with ag(at), but does not reset the nested sphere rolling angles. Neither a source-defined heading nor its deterministic start is established. This is a source-based exclusion, not a claimed failed runtime test.
30 KEEP /Lessons/Games/Slipstream.html — qe.x/z/hd is the player object: br initializes all three; vc updates them; hg reads them to place and rotate the car.. Actual Ec reset invokes original seeded Jm(Rt) and br; world, start grid and 60 source-motion steps reproduce twice in one VM at explicit seed 12345. qe.x/z/hd is the player object: br initializes all three; vc updates them; hg reads them to place and rotate the car. Actual Ec reset invokes original seeded Jm(Rt) and br; world, start grid and 60 source-motion steps reproduce twice in one VM at explicit seed 12345.
31 KEEP /Lessons/Games/Slipstream_GP.html — state.player.mesh.position and state.player.mesh.rotation.y hold the player pose on one existing mesh. placeCarMesh writes them from the authoritative track samples after updatePlayer; updateRace calls it every frame.. Original complete Track including buildMesh gate records, startRace and movement/reset functions executed twice in the same VM with explicit seed 12345 and a fresh default profile; identical track and 60-step pose trace. state.player.mesh.position and state.player.mesh.rotation.y hold the player pose on one existing mesh. placeCarMesh writes them from the authoritative track samples after updatePlayer; updateRace calls it every frame. Original complete Track including buildMesh gate records, startRace and movement/reset functions executed twice in the same VM with explicit seed 12345 and a fresh default profile; identical track and 60-step pose trace.
32 DROP /Lessons/Games/Wrecking_Crew.html — DROP: pM creates y.ball as a spherical physics body. gM steers the rail and applies forces; qf copies body position and quaternion to the spherical mesh. The quaternion is rolling physical orientation, and the source defines no forward axis or player travel/facing heading that AS1 can read without introducing a convention. pM supplies fixed chain positions and new body construction. That does not establish the missing semantic heading field. The per-frame player-heading criterion is not established in the existing interface, so no KEEP or runtime-failure claim is made.
33 DROP /Lessons/Games/Lumins.html — G.lumins contains multiple autonomous agents, each with c/r/dir; the pupil alters terrain and tools. There is no unique player pose to record without first choosing an agent, which this order does not authorize as a substitute. Authored map definitions and original spawn use fixed entrance coordinates; the missing unique player predicate is decisive.
34 DROP /Lessons/Games/Static.html — Evidence-board deduction state has cards, links, verdict and camera pan, not a moving player with heading.
35 DROP /Lessons/Games/OneGuy.html — G.player/makeRunner stores y/vy but no x or heading. Horizontal world position comes separately from worldXAt(G.frame)+PLAYER_X; a complete pose would require an added synthesized state object. Seeded track exists and startRun calls makeRunner/reset cache, but the one-object position-plus-heading predicate fails.
36 DROP /Lessons/Games/The_Last_Lighthouse_v1_1_The_Archipelago_Update_FINAL.html — Boat heading is readable, but start world includes unseeded gameplay fog, not just cosmetic random particles. Lighthouse control angle and lighthouse position are separate objects; reset determinism does not pass.
37 DROP /Lessons/Games/KidsVsStaff_Showdown (3).html — Team quiz/show controller; round progression and scores are not a spatial player state.
38 DROP /Lessons/Games/WorldCup_ThreeLions_Final.html — Classroom tournament scoreboard/challenges with team counters, not a moving football-player object.
39 DROP /Lessons/Games/WorldCup_v3_MatchDirector.html — Match Director is a classroom tournament and scoring controller, not a spatial player simulation.
40 DROP /Lessons/Games/WorldCup_v5_Showdown.html — Showdown player/ball visuals are scripted DOM match animations attached to scoring outcomes; no per-frame player position-plus-heading object.
41 DROP /Lessons/Games/Trekkers_Trail_Runner_Tees_Coast.html — player.position is available; playerMesh.rotation is separately updated. Even treating nested rendered meshes as one reachable object, world obstacle spawning is unseeded Math.random. startGame sets fixed player coordinates and rotations, but updateGameLogic calls unseeded spawnObstacle; reset does not establish a repeatable world.
42 DROP /voxel/ — player contains pos/vel/size/onGround, while yaw and pitch are separate lexical variables. Serialisation explicitly combines them later; they are not one player object.
43 DROP /apexkick/ — simulate precomputes samples with t/pos/vel/omega; there is no per-frame player heading in those objects. Main tick chooses a sample then passes a separate fixed spin axis (0,1,0) and a speed-derived rotation rate to Sc.setBall. Launch azimuth/elevation describe the kick at launch, while replay.angle is the camera orbit. Neither is a current player heading field.
44 DROP /apexpool/ — The ball object has position/velocity plus shotX/shotY (strike direction), not a current per-frame player heading; participant objects contain name/suit/isAI. Aim and participant state live separately from ball position. No current player heading is exposed as required.
45 DROP /apexgolf/ — state.ball contains x/y/z/vx/vy/vz but no current heading; state.aim.angle is a separate aiming object, reset between strokes. Treating stationary aim as per-frame heading would change the criterion.
46 DROP /apextennis/ — G.player has position and stamina/targetX only. Shot angle is generated in separate shotParams/serveParams; it is not a heading on the player object.
47 DROP /neonsync/ — Position and aim share one actor, but the actual start seed varies with wall-clock time. No unchanged fixed-seed start path was established.
48 DROP /biopunkhive/ — Per-frame runtime is economy timers and cosmetic particles. A particle coordinate is not a pupil-controlled player pose.
49 KEEP /neonbreach/ — player.x, player.z, player.yaw (player.pitch also present). resetRunState sets x=0,z=10,yaw=0,pitch=0 and wave 1; arena and nine obstacles are fixed data. Scope: canonical start, not reproducible random enemy choices later. One player object carries live position and heading. Original-source reset/core-start and field-removal control passed.
50 DROP /apexrally/ — Facing is a local renderer value from isAI, not stored on G.player. Shot aim and swing parameters do not supply per-frame player heading on that object.
51 DROP /medevac/ — P is one mission/plane state, but bank and pitch do not provide yaw/heading. No per-frame heading field on the same object was established.
52 DROP /luminahaven/ — Furniture/plants and a decorative cat have transforms; the game is room arrangement, not a single per-frame controlled player actor.
53 DROP /auroralinks/ — Position lives on ball, but no heading/orientation field is supplied there; separate aim is not a heading on the same object.
54 KEEP /neonmeridian/ — player.x, player.y, player.z, player.yaw. City SEED=260811; nodes/chunks/traffic derive from seeded rand; canonical no-query fresh start uses nodes.get(key(0,2)). resetVehicle returns to lastSafe. Fixed initial lastSafe tested. One live player object has full pose and a source-derived deterministic fresh start. VM proves fixed canonical start and recovery from that start; future traffic and visited lastSafe are outside this start-state claim.
55 KEEP /rallyvector3d/ — game.car.pos.x, .y, .z and game.car.heading. Alpine Pass TRACKS[0], seed 9137 and explicit map points; Track builds deterministic samples/bounds; resetStageState sets progress .012 and reads position/heading from that track sample. One car object carries live pose; original Track plus resetStageState passes repeated reset by value and a removed heading assignment turns the measurement red.
56 KEEP /hyperdraft/ — game.player.x, game.player.y, game.player.angle. Measured quick Neon Ring, trackIndex 0, 3 laps. startRace derives raceSeed from track/boss/laps, uses fixed track points and sets a deterministic grid via setCarWorld. One car object carries live pose; original source Track/PuddleManager/startRace repeat by value and removed car.angle assignment fires.
57 DROP /apexcurl/ — The stored stone.angle is spin orientation (angle += omega*dt), not travel heading. No player heading field was established; a velocity-derived angle would be an additional derivation, not the requested exposed heading.
58 DROP /apexvelodrome/ — Rider stores track coordinates but no heading. Rendering reads a separately computed frame angle; the rider object alone does not expose the required pose.
59 DROP /micro-tinkerer/ — Original reset probe restored position but retained a changed heading: Math.PI before versus 0.91 afterwards. Repeatable restart pose not established; no repair made.
60 DROP /townlife/ — World start is not deterministic as written (traffic/pedestrian spawn randomness); on-foot and driving control also switch between two objects.
61 DROP /touchline/ — The user manages a squad. All simulated footballers carry pose, but no one object is the pupil player; choosing a footballer would invent a pilot actor selector not present in the current game.
62 KEEP /skybreak/ — player.pos.x,y,z and player.q quaternion; heading uses original basis(player) then atan2(forward.x,-forward.z). resetGame reseeds 0x5f3759df, clears worldOffset, constructs player and three scouts using original terrain/seeded RNG. Fixed Vanguard/standard loadout measured. Position and full orientation live on one player object; the game itself computes heading from that quaternion. Original reset/initial scouts repeat by value and removing player.q fires.

Count: 62 canonical records; KEEP12; DROP50; publication-only additions excluded7.
CANONICAL MEMBERSHIP GREEN62; delete one record RED61; restore GREEN62.
The prior estate-scope exclusion is released by AS1-R. No active game sweep was evidenced by in-progress runs or sweep commits landing within24hours.
STOP AND REPORT — Recommended: Rally Vector3D, existing Alpine Pass. Second choice: Hyperdraft, existing quick Neon Ring. Matt has not named the pilot.
Rally Vector3D has game.car.pos plus heading and its own deterministic Track/resetStageState. Hyperdraft has game.player.x/y/angle and a track-derived deterministic startRace. Original-source VM checks for both were GREEN, missing heading RED, restored GREEN.
No game edits; no §6 work. Proceed only with the independent HUD census; §3 remains held for both Matt decisions.
