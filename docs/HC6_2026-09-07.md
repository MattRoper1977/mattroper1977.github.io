# HC6 — teaching-pack layer, pin chain and HC5 residue (2026-09-07)

Order: `mbm-finish-2026-09-07-HC6`. Exit token at the foot. Every section ends CLOSED, PARTIAL, BLOCKED or HANDED.
Instruments were run before conclusions were written; where an instrument was wrong first, the wrong reading is kept
beside the right one (§25 of the readback).

## Rollback anchors (mains at HC6 start)

| repo | main at start | main at close |
|---|---|---|
| Site | `305b7dbf` (#310) | `e43ef0fa` (#314) |
| Lessons | `8a74dab7` (#374) | `1c2505f7` (#379) |
| Apps | `5faeaeb9` (#57) | `65015ed5` (#61) |
| Games | `4e318bdf` (#73) | unchanged |

## Part A — HC5 residue

### §1 Pin chain (CLOSED — pin 6 published; Humanities and the teaching-pack layer WITNESSED)
- Pin 4 (Site `09a44fae`, the #308 merge) landed in Lessons (#375 → `c4cfa942`) but **`09a44fae` is not fetchable by SHA**
  (`upload-pack: not our ref`, measured from the session and twice in CI: Lessons publication run `34131331210`
  resolved 0 jobs; Apps #58 verify died at checkout on both attempts). Neighbouring merge commits fetch normally.
- Pin 5 → Site `305b7dbf`: Lessons #376 (→ `0499b63f`), Apps #59 (→ `33d2ae0d`); Apps #58 closed superseded.
  Both callers pre-flighted locally through the 305b7dbf publisher steps before the push.
- Local pre-flight then showed the publisher rejecting Lessons main on `check_education_separation.py`
  ("Installed combined registry records changed"): the frozen usage-registry baseline predates the Humanities packs
  (138 added rows, all under `/Lessons/Humanities_Teesside/Teaching_Packs/`). That is the real reason the Humanities
  packs never served, not the pin. Repaired on the Site side in #311 (merged `47c47569`, §11 below). Pin 6 → Site `47c47569`: Lessons #379 (→ `1c2505f7`), Apps #61 (→ `65015ed5`); fetchability checked first. Witness subjects: Site #314 (→ `e43ef0fa`).
- Site #304 merged (`6e55e5f7`) after a cancelled (not failed) check was re-run; Site #25 closed with it (D1).
- #309 → `9484b09b`, #310 → `305b7dbf` (HC5 close).

### §2 Required-check repair (CLOSED, D3)
"Fetch the live estate and compare to raw-at-SHA" now reproduces the PR's merge ref as a candidate; main's
reproduction is `continue-on-error`; the candidate stands in as the subject only when main's build is broken.
Repair PR Site #312 (→ `ee13d41b`) passed the check normally. Control PR Site #313 (planted stale Play evidence,
closed unmerged, head `8dbdc860`) went RED at step 18 "Reproduce the candidate publication from the merge ref"
with main's own reproduction green at step 13. The check stays required; the only workflow edit of the order.

### §3 Size (CLOSED for Humanities, NOT DONE for Science — D2)
30 duplicate Humanities week zips removed (5 × `W<n>_Teaching_Pack` + 5 × `<slug>_Lesson_Pack` per pathway); every
file in them is in the Complete pack and the format collections. Index, manifest, SHA256SUMS updated.
`Humanities_Teesside/Teaching_Packs` in the tree: **132,106,707 → 87,544,140 bytes** (−44,562,567). Repository
history untouched (no rewrite), so the repository's object store keeps the bytes; only the checkout and the
published tree shrink. Science week zips stay: the whole-estate grep found references in
`tools/verify_cross_estate_unification.py`, `tools/catalogue/pin_catalogue_contract.py`,
`Science_Teesside/Teaching_Packs/*/DOWNLOAD_INDEX.json` and the Site usage registry — not zero.

### §4 Decks (HANDED — VB)
No VB queue file exists under `_sownb/vb/` (VB_STATE run 14; campaign PAUSED at VB_EASTER_A3_PAUSED). The
≤8-line VB-EASTER-A3 amendment stanza is readback line 26.

### §5 Splash (CLOSED)
`tools/render_maker_splash.py --root . --check` on Site main `ee13d41b`: **18 applied targets; 0 written; 0
divergent**. Idempotence: `--write` twice on a scratch copy of main wrote 0 both times. Focus gate on both viewports
and the inert-subtree guard are `tools/verify_maker_splash.mjs`, green on main: run `34134702723` (push, `6e55e5f7`)
and `34131859341` (deployment, `305b7dbf`). No regeneration PR was needed: the 18 already reproduce byte-for-byte.

### §6 Pins (HANDED — M3)
Games workflow permission is human-only (M3, tracking issue Games #74). Neon Breach stays pending until the Play pin.

### §7 Biopunk (HANDED — D6)

### §8 PR table — readback line 23.

## Part B — the teaching-pack layer

### §9 Intake (CLOSED)
Ten zips, 528 rows classified by script (`hc6/intake.py` → `intake.json`); 219 HTML lessons; predictions met.

### §10 Named fixes (CLOSED, script-applied, logged in `fixlog1.json`)
P1 logo → estate inline SVG mark (gate: 0 data-URI images >10 KB after; screenshot pair). P2 `[Sources]` → list in
the teacher region. P3 furniture through the canonical NAV-1 writers (131 pages; way-home pinned visible where focus
scrolls the topbar off, measured). P4 one-truth substitutions (chassis companion replaces pack HTML for the same
lesson id in the tree and in the zips; pack originals not published). P5 links: 2,417 references across the new
roots, 0 broken (the six pre-existing `/hud.js` references in the live FW pages are Matt's). P6 slot strings verified
against the timetable. P7 orphan DOCX media stripped; PDFs regenerated only for edited files.

### §11 Landed (Lessons #377 → `d4e9085f`, #378 → `f395f49d`; Apps #60 → `af09c14a`; Site #311 → `47c47569`)
Tree: `<subject root>/Teaching_Packs/<PATHWAY>/{index.html, guides as HTML, HTML/, PowerPoints/, Slide_PDFs/,
Documents/, Resources/, Starter_Files/, downloads/ (Complete + PPTX/DOCX/PDF only), SHA256SUMS.txt}` for Careers
(BUILD, GROW), ICT (GROW), DT_Textiles (BUILD, GROW), BUILD_ASDAN/FoodWise (BUILD), GROW_FoodWise (GROW), PSHE (GROW);
Science and Humanities gain `web-slides.html` companion views (D10c, no catalogue rows); landing
`Lessons/Teaching_Packs/index.html`; root card "Editable teaching packs · 7 subjects"; catalogue 743 → 751.
D10b: the six BUILD FoodWise chassis lessons sit beside the live `FW_W*.html` as `BUILD_FOOD_W1..6.html`, linked
from the pack index, not from the catalogue; `data-style="recommended"` NOT applied (Site admission pending pin 6).
D11: Website_Ready compared path-by-path to the pack zips — 78 shared files identical, 0 differ; no STOP.
D12: every card line is a verbatim quote from the pack's README/START_HERE, asserted at generation.
Science route census: the 20 companion web-slide lessons recorded as retainedAlternatives (35 → 55).

### §12 Derived surfaces (PARTIAL)
Site #311: catalogue mirror 663 → 737 rows (the Site's own pinned Lessons source, 419d44f0 — the discovery gate
compares the index to that pin, so the mirror cannot run ahead of it), search index regenerated through the
generator's `--write` path, generator learns the lower-case `hub` row type. The 751-row mirror advances with the
next Lessons pin bump in the Site — a workflow edit (`domain-split-verify.yml`, `published-completion-verify.yml`, `education-publication.yml`), which D3 forbids this order from making: HANDED.

### §13 Gates — readback line 24.

## Readback (30 lines, in the order given)

```
1 HC6_PARTIAL
2 rollback: Site 305b7dbf→e43ef0fa · Lessons 8a74dab7→1c2505f7 · Apps 5faeaeb9→65015ed5 · Games 4e318bdf→4e318bdf (untouched)
3 #308 at 13:53: MERGED by Matt 13:41 (09a44fae); that SHA is NOT fetchable by SHA (upload-pack: not our ref) → pin 4 dead on arrival, pin 5 → 305b7dbf (Lessons #376→0499b63f, Apps #59→33d2ae0d)
4 M3: N (Games workflow permission still human-only; issue Games #74 open)
5 VB: run 14 · PAUSED (VB_EASTER_A3_PAUSED) · no queue file under _sownb/vb → stanza at line 26
6 pin: #4 (09a44fae) unfetchable (Lessons run 34131331210 zero jobs; Apps #58 checkout ×2) · #5 305b7dbf merged · #6 → 47c47569 (Lessons #379→1c2505f7, Apps #61→65015ed5); Lessons publication run 34141118076 GREEN at 1c2505f7; Apps run 34141126451 GREEN at 65015ed5; Site own publication 34140324890 GREEN at 47c47569
7 Humanities SERVED 24/24 Lessons subjects (incl. all 5 Humanities pack subjects) — witness run 34141709385 on Site e43ef0fa, Lessons deployment 34141118076 at 1c2505f7, byte-matched against the origin fetch at that SHA
8 size: Humanities_Teesside/Teaching_Packs 132,106,707 → 87,544,140 B in the tree (−44,562,567; 30 zips); repo history unchanged (no rewrite); Science dedupe NOT done (references found in tools + Site usage registry)
9 check repair: #312 merged ee13d41b (merge-ref candidate); control #313 RED at step 18 "Reproduce the candidate publication from the merge ref", closed unmerged; required, unchanged otherwise
10 #304 merged 6e55e5f7 · #309 merged 9484b09b · #310 merged 305b7dbf · #25 closed with #304
11 witness: run 34141709385 — 3/4 publications WITNESSED: site 4/4 · lessons 24/24 (teaching-pack layer + Humanities packs) · apps 1/1 · games 0/2 RED "served from another origin" (bytes equal, origin is the Site host: M1 hosting, pre-existing)
12 splash 18/18: render_maker_splash --check on ee13d41b: 18 applied, 0 divergent; --write ×2 on a scratch copy: 0 written; verify runs 34134702723 (push) · 34131859341 (deploy) green
13 pins: HANDED (M3) · Neon Breach pending the Play pin · Games #74 open
14 Biopunk: HANDED (D6)
15 census: 10 zips · 528 rows classified by script · 219 HTML lessons (Careers 14, ICT 7, Textiles 6, FoodWise 10, PSHE 7, Science 20, Humanities 15 companions, + resource/teacher views) · predictions met
16 one-truth: chassis companion replaces pack HTML for the same lesson id in tree and zips; pack originals not published; Website_Ready vs pack zips 78/78 identical, 0 differ → no STOP
17 P1–P7: P1 logo→inline SVG (0 data-URI >10 KB) · P2 sources → list in teacher region · P3 furniture 131 pages · P4 substitutions · P5 2,417 refs 0 broken (six pre-existing /hud.js in live FW pages are Matt's) · P6 slot strings verified · P7 orphan media stripped, PDFs only for edited files
18 furniture: way-home NAV-1 + splash on 131 chassis pages (fixed-position way-home where focus scrolls the topbar off, measured); guide toggle none (0 measured surfaces); T2-4 none (0 ASDAN evidence surfaces)
19 landing/indexes live: WITNESSED on madebymatt.uk (landing, 7 subject indexes, 2 web-slide views, Careers BUILD pack index, BUILD_FOOD_W1.html, PSHE Complete pack) — Lessons/Teaching_Packs/index.html + 7 subject indexes + 13 pathway indexes rendered 390/1280: 0 overflow, 0 failed requests, anchors all resolve
20 catalogue 743→751 (8 rows; g8 red-proved with a planted ninth row) · root card "Editable teaching packs · 7 subjects"
21 FoodWise BUILD admission: six chassis lessons beside the live FW_W*.html as BUILD_FOOD_W1..6.html, linked from the pack index only; data-style="recommended" NOT applied (Site exact-byte admission lands with pin 6; live FW routes stay recommended)
22 links: 0 broken across the new roots (2,417 refs); external hosts (12, led by madebymatt.uk ×121, nationalcareers.service.gov.uk ×12, gov.uk ×8): two CI readings an hour apart NOT done (no Lessons-side link workflow; Site's bounded external-link check covers Site pages only)
23 PRs: Lessons #375 M(c4cfa942) #376 M(0499b63f) #377 M(d4e9085f) #378 M(f395f49d) #379 M(1c2505f7) · Apps #58 closed-superseded #59 M(33d2ae0d) #60 M(af09c14a) #61 M(65015ed5) · Site #304 M(6e55e5f7) #311 M(47c47569) #314 M(e43ef0fa) #312 M(ee13d41b) #313 closed-unmerged(control) #25 closed · Matt-marked untouched: Site #291 #109 #106 #91
24 gates: g8 catalogue red-proof ✓ · D12 trace assertion ✓ · D11 path identity 78/78 ✓ · render 390/1280 0 overflow ✓ · links 0 broken ✓ · Site admission controls 55 PASS both builds ✓ · separation self-test 14 PASS ✓ · discovery gate PASS (618 pupil routes) ✓ · typed-literal census 0 live-unbound ✓ · D3 control red ✓
25 instruments wrong before right: (a) pin 4 assumed fetchable — it was not (measured after two CI deaths); (b) hc6_indexes D12 lines typed by hand — replaced by lines derived from pack text; (c) first "final" Site build taken from the content branch, not the catalogue branch — pairs re-cut; (d) GLV3/static-contract reds on #378 were the stacked content diff, gone after #377 merged; (e) mirror at 751 failed the discovery gate against the pinned source — set to 737
26 VB-EASTER-A3 amendment (HC6 D4, 6 lines): (1) add plan item A3-H9 — the 16 Humanities Autumn 1 decks (BUILD/GROW/LAUNCH W3–W7 + Review) carry the HC5 H1–H4 XML fixes; VB owns any re-render; HC6 touched no deck. (2) inputs: Humanities_Teesside/Teaching_Packs/<PW>/Week_n/*.pptx at Lessons main d4e9085f; fix log _packs/HUMANITIES_A1_FIXLOG.md. (3) the 30 week zips are gone (D2) — the Complete pack and the format collections are the artefacts. (4) no re-wording, no re-sequencing, SEMH rules preserved. (5) g18_v2 family floor unchanged. (6) run only after CAMPAIGN_STATUS leaves PAUSED.
27 MATT TAPS: M1 hosting · M3 Games workflow permission · M4 Supabase · phone pinch check
28 residue: Site Lessons-pin bump 419d44f0→post-#378 + mirror 751 (follow-up Site PR); Science week-zip dedupe (references exist); external-link two readings an hour apart in CI — not done
29 NOT done: data-style="recommended" on the six beside-placed BUILD FoodWise lessons (admission now passes; applying it is a teaching-default change for Matt to tap); Science week-zip dedupe (references exist, D2 condition unmet)
30 NOT done: Games served-from-own-origin (M1) and the Play pin (M3) — human-only; Biopunk HANDED (D6); the 16 decks stay with VB (line 26)
```

Token: **HC6_PARTIAL** — see readback lines 28–30 for what is not done.
