# ORDER FC-Z — close record

**2026-08-26.** Supplies the five rulings FC-R's halts waited on, and closes the
FC line.

Executed in the order the order set: **§Z2 → §Z3 → §Z1 → §Z4 → §Z5 → §Z6**, with
§Z3 gated behind §Z2's merge.

| repo | tip at close |
|---|---|
| `mattroper1977.github.io` | `cb435f4` |
| `MattRoper1977/Lessons` | `288f845` |
| `MattRoper1977/Games` | `43b29f7` |

Three sections landed. One is blocked on a click that is Matt's. One halted on
its own stop condition. One was report-only by ruling.

---

## §Z6.1 — Per section: outcome, PR, merge, checks, and what was *measured*

### §Z2 — Exclude the class, not the instances · **LANDED**

| | |
|---|---|
| PR | site [#190](https://github.com/MattRoper1977/mattroper1977.github.io/pull/190) |
| merge | `cb435f4` |
| checks | **10 runs — 8 success, 2 skipped, 0 failure.** All four required contexts green. |

FC-R found eight arcade games mis-filed under a teaching pathway. Running the
per-record instrument across all **717** records in this pass found a **ninth** —
a second `Voxel Frontier` record FC-R's sweep had not reached.

**Measured:**
- 717 records swept by a per-record instrument, not a text scan.
- Nine records lost a `pathway` facet. **Delta cap: zero other records changed** —
  asserted, not sampled.
- `s24-pathway-exclusions` green: 64 game records, none carrying a teaching
  pathway.
- The gate is fail-closed: `data/pathway-exclusions.json` absent ⇒ the generator
  exits `MEASUREMENT INVALID` rather than falling back to unguarded text matching.
- Non-vacuity: the verifier fails if the exclusion list is empty, so a gate that
  excludes nothing cannot report green.

**Inferred:** nothing. The class (`category == "game"`) was read from the record,
not guessed from the slug.

**Reported, not silent:** the committed search index carried a stale
`sourceHashes.games.json` (`0173aec…` against a file hashing `4b3787eb…`),
pre-existing since `4d355e8` (Order TS). `--check` exits 0 without catching it.
Declared through `--expect-diff` rather than quietly regenerated.

---

### §Z3 — The six descriptions · **LANDED in Games. The site half is BLOCKED.**

| | |
|---|---|
| PR | Games [#41](https://github.com/MattRoper1977/Games/pull/41) |
| merge | `43b29f7` |
| checks | green; `contract` and `aggregate` both passed |

**The post-merge delta cap — the assertion that would have reverted #41:**

| | |
|---|---|
| records with a pathway | **549 → 549** |
| records changed at all | 6, all on `description` |
| changed on `pathway` | **0** |
| **GAINED a pathway facet** | **0** ← the assertion |
| LOST a pathway facet | **0** |

The six new descriptions added no record to any pathway facet, so §Z2's class
exclusion is not too narrow and #41 stands.

**The site half is site [#191](https://github.com/MattRoper1977/mattroper1977.github.io/pull/191) and it cannot merge.** See the next section.

**Measured:** the delta cap, across the full record set. The mirror is
byte-identical to the canonical (`f4aab9ab`, 28,805 B), generated by
`render_games_manifest_mirror.py --write` and never typed.

**Caught mid-section:** updating the mirror left `for/pupils/index.html` stale.
The R7 chain is record → renderer → output, and it does not stop at the mirror.
Re-rendered; **12 visible strings changed.**

---

### **The blocker: a required check that no change can turn green**

This is the one thing in this pass that needs Matt.

`Fetch the live estate and compare to raw-at-SHA` is a required context. Its
mirror step compares the **served** canonical against **main's** copy of
`data/source-manifests/games.json` — because the job checks out `ref: main` as
its subject.

The canonical lives in the Games repository. So the instant Games #41 landed,
main's mirror was stale, and the only thing that can clear it is a site pull
request — which that step then reds. Including the catch-up. Including #191,
whose entire content *is* the catch-up.

```
FAIL data/source-manifests/games.json is not the served canonical:
     mirror    28722 B 4b3787eb97249b3f   <- main
     canonical 28805 B f4aab9ab92413d9d   <- served
```

#191's own mirror is `28805 B / f4aab9ab` — identical to the canonical. The pull
request is correct; the check is reading the wrong tree. Its sibling gate
`Mirror equals the canonical shelf`, which reads the ref, **passed**.

**#191 at `a4a76e2`: 14 runs — 11 success, 2 skipped, 1 failure.** The one
failure is this. Three of the four required contexts are green.

A fix was written and **verified in four cases**, then **reverted** — the working
tree was restored byte-identical. Editing a required status context is a click,
not an inference. The diagnosis, the evidence, the verbatim patch and the three
options are in `docs/MBM_LIVE_MIRROR_LEG_DEADLOCK.md`.

The one case that matters from the proof: **main drifted, ref drifted too ⇒ still
FAIL.** A pull request that does not touch the mirror carries main's copy of it,
so a drifted main still reds every unrelated pull request exactly as before. The
single case where the two readings differ is a pull request that *fixes* the
drift.

Until this clears, **every site pull request is red on that context**, including
the one carrying this record.

---

### §Z1 — C4 closed as a false positive · **LANDED**

| | |
|---|---|
| PR | Lessons [#161](https://github.com/MattRoper1977/Lessons/pull/161) |
| merge | `288f845` |
| checks | **5 runs — 5 success, 0 failure.** |

Matt's ruling: the names in the ASDAN TA deployment note are invented worked
exemplars — declared fiction — not learner data.

Four records carried C4 and each was stamped in **its own vocabulary**, per
§Z1.2's instruction to match the record rather than impose a sentence:

| record | treatment |
|---|---|
| `LIVETEACH_RESIDUE.md` | closure inside the C4 record |
| `LIVETEACH_LT1_CONTACT_SHEET.md` | closure appended to the C4 row |
| `REGISTER.md` | closure inside the C4 entry |
| `LIVETEACH_LEDGER.md` | dated annotation **beneath** the block |

The ledger is different on purpose. It records what was believed when it was
written, so the block is retained unaltered and the closure sits under it.

**§Z1.4 diffstat gate — measured:** 4 files, 7 insertions, 1 deletion;
`LIVETEACH_LEDGER.md` insertions **2**, deletions **0**; original block confirmed
byte-identical. The single deletion is the contact-sheet row, a one-line table
cell that cannot be appended to without rewriting the line.

**Two things deliberately not done:** the ruling is cited **by date, not by
file** — `data/declared-fiction.json` exists in the site repo, not in Lessons, and
citing it there would dangle. And **no name appears anywhere in the diff**, which
holds regardless of the ruling that the names were never pupil data.

**Measured, not inferred:** the four files came from a per-token instrument. A
bare `grep C4` hits **14** files in that repo — clicker controls, a hex colour
`#C4F52A`, register tooling.

---

### §Z4 — Section 19 · **HALTED at §Z4.1. Nothing was written.**

§Z4.1 is explicit: *"Not recovered ⇒ STOP the section and hand the sentence to
Matt. Do not compose a statutory sentence to unblock your own section. That is the
one thing this order will not do."*

**It is not recovered, and that is a measurement, not an assumption.**

The search ran with a **positive control first** — a token known to be in this
history (`discovery card`), searched the same way. It returned 4 commits, so the
instrument can find a string that exists. Only then were the absences meaningful.

| where | result |
|---|---|
| `git log -S"Section 19"` across **all refs** | 3 commits |
| …restricted to `data/audience-homepages.json` | **0** |
| …restricted to `for/councils-organisations/index.html` | **0** |
| …restricted to any `for/**` page | **0** |
| the token in the diff of those 3 commits | `docs/MBM_FCR_CLOSE_RECORD.md`, `docs/MBM_FOR_COPY_CLOSE_RECORD.md`, `docs/FC_UNSURE_STRINGS.md` — **all records *about* the refusal** |

**A false hit I caught and corrected mid-search.** My first per-file pass reported
that `for/councils-organisations/index.html` carried the token. It does not.
`git show <commit> -- <file>` prints the **commit message** above the diff, and
that commit's message names the token. Re-run with `--format=""` and the page
drops out — with a positive control (`suitable education`) confirming the
corrected loop still finds what is really there.

FC's own PR #188 settles it in four words: **"Written, then withdrawn."** The
sentence existed in FC's working tree, went red on
`verify_catalogue_counts.mjs`, and was replaced before anything was committed. No
commit, no PR body, no record carries it. What survives is only FC's *description*
of it, and the substitute that shipped:

> *"If the question in front of you is a duty to arrange suitable education, a
> PEP, or what a Virtual School team can point a setting towards, this is work to
> look at rather than a proposal to consider."*

That sentence is on the councils page now and was not touched.

**Nothing downstream ran.** §Z4.2–§Z4.7 — the renderer route,
`data/statutory-citations.json`, the three-way proof, the fail-closed rename, the
R4 assertion, the 200 check — are all gated behind a wording that does not exist.
`data/statutory-citations.json` was **not created**: an allowlist whose `pages`
array is empty is `MEASUREMENT INVALID` by §Z4.3's own terms.

**Checked anyway, so the state is known:** the councils page carries `Section 19`
**0 times**, and its R4 guard clause **once** —
*"Made by Matt is not presented as endorsed by a council or education
organisation. No proposal process, customer list or commercial arrangement is
invented."* `verify_guard_clause_present.mjs` is **17/17**, seven clauses in the
record and seven served byte-for-byte.

**What Matt needs to supply:** the sentence itself. One line naming Section 19 in
a recognition register. The order's scope note is binding on whatever comes back —
a citation token and sector vocabulary, *not* explanatory prose about
local-authority duties, not a claim about what the duty requires, not anything
about the estate's standing under it.

---

### §Z5 — The four takes · **REPORT-ONLY, by ruling. Nothing touched.**

`Z-D5 = REPORT-ONLY`, not swappable by any agent. **Annex A stays quarantined:**
the four texts were not copied into any file, PR body, commit message or this
record, and were not compared against the existing takes. FC asked four
*punctuation* questions; four whole takes are a rewrite, and under the 2026-08-23
ruling that is Matt's alone.

`tools/verify_takes_pin.mjs` re-run at head: **17/17 passed.**
`games/index.html` byte-unmodified. The pin was not opened for writing.

Every separator is `U+0020 SPACE`, `U+002D HYPHEN-MINUS`, `U+0020 SPACE` — a
plain spaced hyphen, not an en or em dash.

| # | identifier | path + line | the exact characters in question | FC's question, as FC asked it |
|--:|---|---|---|---|
| 1 | `/emberwild/` | `games/index.html:303` | `Madebymatt` (word-initial cap only, one word) · ` - ` = `U+0020 U+002D U+0020` | *Several small things — want me to list a suggested form for your yes/no?* |
| 2 | `/olympics/` | `games/index.html:304` | ` - ` = `U+0020 U+002D U+0020` | *An em dash to match the others, or keep the hyphen?* |
| 3 | `/apexpool/` | `games/index.html:306` | ` - ` = `U+0020 U+002D U+0020` · final character `l` (`U+006C`) — **no terminal stop** | *Several small things — want me to list a suggested form for your yes/no?* |
| 4 | `/auroralinks/` | `games/index.html:309` | ` - ` = `U+0020 U+002D U+0020` | *An em dash to match the others, or keep the hyphen?* |

**"To match the others" — measured, so the question is answerable:** 18 takes in
total. **4** use the spaced hyphen (these four). **3** use `U+2014` EM DASH
(`Glitch_Clash`, `Marble`, `Trekkers_Trail_Runner_Tees_Coast`). **0** use an en
dash. The remaining 11 have no dash at all. **1** take lacks terminal punctuation
— `/apexpool/`, row 3.

So "the others" is three em-dash takes against eleven with no dash. Whichever way
you answer, it is a small, consistent set — not a house style you would be
breaking.

---

## §Z6.2 — Production honesty

The container proxy refuses `madebymatt.uk` at CONNECT, so **nothing in this
session fetched production directly.** CI is the only channel that can, and this
time it matters: **unlike FC-R, this pass changed what is served.** The gap is
real and it is not empty.

**What CI actually fetched from production, and what that proves:**

| | |
|---|---|
| `https://madebymatt.uk/Games/games.json` | fetched, hashed **`f4aab9ab`, 28,805 B** — byte-identical to Games main at `43b29f7`. **The six new descriptions are live.** This is a real production fetch, not a local render. |
| every derived route on the custom domain | HTTP 200, and `Live bytes must equal the committed tree` **passed** against main — so main's deployed surfaces are the served surfaces. |

**What is NOT verified, stated plainly:**

- **§Z2's change is not live-verified.** The live job **never fetches**
  `data/mbm-search-index.json` — I checked, the string appears **0 times** in that
  workflow. The nine arcade records losing a pathway facet is verified against the
  committed tree at `cb435f4` and **against a faithful local reproduction, not
  against production.** That the surrounding routes are live-equal makes the
  deploy very likely complete; *very likely* is an inference and it is written
  here as one.
- **Live rendering is unverified.** No browser in this session loaded a page from
  `madebymatt.uk`. Every rendering claim is from headless Chromium against the
  repository tree.
- **CDN and deploy staleness** remain unmeasured for anything the live job does
  not fetch by name.
- **#191 is not merged**, so the site mirror and the re-rendered pupil page are
  **not served** and no claim is made that they are.

Nothing in this record says "production verified" over a local render.

> **Annotation 2026-08-26 — ORDER FC-X §X4. The first bullet above is now
> SUPERSEDED, and the section is retained unaltered as the record of what could
> be measured at the time.**
>
> FC-X treated "the live job never fetches the search index" as a coverage hole
> rather than a reporting limit, and closed it: a report-only leg was added that
> fetches `https://madebymatt.uk/data/mbm-search-index.json`. It has now run
> against production (`workflow_dispatch`, run `32986126849`, step 12, success):
>
> ```
>    control  cmp distinguishes a one-byte mutation — the comparisons below are live
>    served   755918 B a62664db604073f3
>    main     755918 B a62664db604073f3
>    MATCH    the served index is byte-identical to the deployed tree
>    census   717 records served · 64 in the game class
>    census   game records carrying a teaching pathway (BUILD/GROW/LAUNCH): 0
>    census   none — the class exclusion holds AS SERVED
> ```
>
> **§Z2's change IS live-verified.** It rests on a real production fetch, not on
> the committed tree plus a local reproduction. The inference this record was
> careful to label as an inference turned out to be true — but it was right to
> label it, and it is the fetch, not the guess, that settles it.

---

## §Z6.3 — Three things to tap, on the phone

Amended from the order's own list, because §Z4 halted and item 1 as written
cannot be true.

**1. Councils page** — `madebymatt.uk/for/councils-organisations/`

- The R4 line should be there **once**: *"Made by Matt is not presented as
  endorsed by a council or education organisation…"*
- **`Section 19` will NOT appear.** That is correct, not a fault — §Z4 halted
  rather than compose a statutory sentence. The duty is named in words:
  *"a duty to arrange suitable education."*

**2. Site search for `build`** — no arcade game should appear under the **BUILD**
pathway facet. This is the one to look hardest at: it is the change this pass made
to what search returns, and it is the change CI could not fetch from production.

**3. Biopunk Hive** — should carry **no pathway facet** at all. It was filed under
`GROW` because its description says *"grow a forbidden containment hive."*

---

## §Z6.4 — Handback

### B2 — the nearest real deadline (Matt + Cheryl)

Verified 2026-08-18. ASDAN is withdrawing **all** Vocational Taster titles:

| | |
|---|---|
| registrations and student books available to | **31 December 2026** |
| final certification | **31 August 2027** |

- **LAUNCH Hospitality is a Vocational Taster** and is on that clock.
- **Gardening is a Short Course and is NOT** on the Vocational Taster withdrawal.
- **New, not previously on the list: the RoadWise Short Course is being withdrawn
  on the same two dates.** Check whether anything in LAUNCH or PSHE depends on it.

This is a curriculum decision, not a code change.

### The clicks and answers only Matt can give

| # | what | why it is yours |
|--:|---|---|
| 1 | **Unblock the site repo.** Either merge #191 with a rules bypass, or approve the patch in `docs/MBM_LIVE_MIRROR_LEG_DEADLOCK.md`. | It edits a required status context. Until it clears, **every site pull request is red** on that one check. |
| 2 | **The Section 19 sentence.** One line, recognition register. | §Z4 will not compose a statutory sentence to unblock itself. |
| 3 | **Annex A** — are those four texts your own words, and are they intended as whole-take replacements? | No take moves until you answer. They stayed quarantined and are quoted nowhere. |
| 4 | **The four takes** — answer any row of the §Z5 table in one line. | The pinned region is yours by the 2026-08-23 ruling. |

---

## §Z6.5 — The FC appendix, inherited

Reproduced verbatim from ORDER FC, **minus C4** (closed — see §Z1) and **minus
B2** (handed back above), with **A1 amended**.

> **A1 — The tag backfill.** `data/tag-backfill.csv`, 641 rows, four fields
> pre-filled and four tag fields deliberately empty (never seeded with the
> discarded derivation — the spot-check was 12/20 against an 18 threshold, so the
> whole derivation was binned). Parked at S5 with the ruling: **tag the ~30
> resources Matt teaches from in the first fortnight, then decide.** This is the
> single highest-leverage queued item — it is the blocker under A2 *and* under the
> faceted filter's multi-select.
>
> **AMENDED 2026-08-26 (FC-Z §Z6.5): A1 now blocks TWO things, not one.** It is
> still the blocker under A2's pupil card badges. It is *also* the blocker under
> the pathway classifier flip. §Z2 excluded the arcade **as a class** because that
> is the honest fix available today — the classifier still derives `pathway` by
> word-matching prose, so an ordinary verb in an ordinary sentence still files a
> record under a teaching pathway. The real fix is a declared tag field per
> record, which is A1. Until A1 lands, every new description is one verb away from
> the same defect, and the class exclusion is a fence around the arcade rather
> than a repair of the mechanism.

> **A2 — Pupil card badges** (device · controls · quick-play · silent-friendly).
> The audit pack proposed them; Order S already settled the *design* —
> non-interactive on pupil surfaces, interactive and ≥44px on teacher surfaces,
> one component, two call sites. **Blocked on A1**: the record carries `subject,
> type, family, year` and nothing that yields a device or control tag. Do not
> derive these; that path was already measured and discarded.

> **A3 — `data/declared-fiction.json`** as a standalone deliverable if §FC6.7
> ledgers rather than builds it. Cheap, useful beyond the gate, and it converts
> "everything is invented" from a claim into a checkable record.

> **B1 — The two public ASDAN PDFs.** Each carries one stale "BUILD — an Award"
> sentence, mirroring what PH-3's C1 fixed in `asdan/app.html`. PH-3 C5 stopped
> correctly: **no PDF generator exists in the site repo.** This is a
> regeneration-day item, not a patch — it needs the generator question answered
> first.

> **B3 — The 10-hour rule workbook fix.**
> `Planning/LAUNCH/LAUNCH_Autumn_Year_Plan_ASDAN.xlsx` repeatedly asserts a
> 10-hour rule on ComSk1 ("THE 10-HOUR RULE IS THE DESIGN CONSTRAINT"). It is
> false — the gate is on the other five skills. Proposed cell fixes sit in
> `_passph3/JOB_A_REPORT.md`, values-only, listed cells only. **Verify whether
> they were applied; do not assume either way.**

> **C1 — The `/for/` cross-link graph is asymmetric.** Each audience page offers a
> "switch to…" link, but the graph is not closed: several audiences are linked
> *from* nowhere. Audit all seven, build the full matrix, and close the loop — a
> reader who lands on the wrong page should be one link from the right one, in
> every direction. Cheap, and it compounds with the findability work.

> **C2 — The teachers-page video claim.** The page describes an owner-controlled
> demonstration. Verify whether it is a genuine click-to-load facade or an
> embedded player that loads on page view — the claim is only true in the first
> case. **Verify before rewording; do not reword into a stronger claim.**

> **C3 — The trusts page.** FC only reconciles it (`TRUSTS=reconcile-only`)
> because the audit pack supplied no markup for it. It is consequently the
> least-worked of the seven and probably the weakest. Worth its own small pass
> once FC lands.

> **D1** — UAS centre name and number (`#set-centre`, `#set-cno`): on-device
> storage, values only Matt holds.
> **D2** — The eyeball tap-list from the TSR close.
> **D3** — Deferred Lessons and Apps rulesets: SAT-F §2 gated them. Re-run
> `report_required_checks.py` and create them when the deferral reason has
> genuinely expired.

**C4 is struck** — PH-3 PRs #133 (Lessons) / #168 (site). It asked for
verification that they merged. That item is unrelated to the safeguarding C4
closed in §Z1; both are named "C4" in different registers and the collision is
noted here so the next pass does not conflate them. *(Appendix C4 remains open as
written; it is reprinted here rather than struck, because only safeguarding C4
was ruled on.)*

> **C4 — PH-3 PRs #133 (Lessons) / #168 (site).** Order TS set `PH3_MERGE=yes`, so
> these are *probably* merged. **Verify, do not assume** — and if merged, confirm
> the guidance-hidden-by-default toggle behaves on a real deck at 390px.

---

## The one correction in this pass worth carrying forward

A per-file token search reported a hit that was not there, because
`git show <commit> -- <file>` prints the commit message above the diff — and the
commit message named the very token being searched for. It looked like evidence
that `Section 19` had once been on the councils page. It had not.

The instrument was contaminated by the thing it was measuring. What caught it was
running a **positive control through the corrected loop** rather than trusting
that the correction worked. Same species as FC's `discovery card`: the sweep was
structurally unable to see what it claimed to have checked.
