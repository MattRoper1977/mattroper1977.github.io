# HC3 served pinch-zoom recovery — 2026-09-07

Rollback before implementation: Site38630cbf05c6272631e4c69828144afca174d4e0.
No app or game source changes. The old pinch instrument was not recovered from
Claude's retained branches/artifacts. Reconstruct only the read-only verifier.

Derive affected files from merged Apps#37–40, Site#278 and Lessons#350, then
reconcile every route with the canonical Apps manifest or69-route game census.
Recover the successful exact-source Apps and Games publication artifacts, bind
each to its upload/deploy attempt, and compare fetched HTML with those bytes.
Measure actual Chromium touch pinch on the published app/game interface at390px,
plus desktop load/usable navigation. Use a real unblocked control, one planted
viewport/touch block that must prevent zoom, then restore; report missing or
failed targets by route. No source injection into the live pages. Preserve console,
network and screenshots as findings; arbitrary pre-existing findings are not green.

The workflow checks the candidate instrument against the active publications. Its
record distinguishes instrument SHA, source main SHA, publication SHA and live
bytes. Missing/expired publication evidence is INCONCLUSIVE, never replaced with
a new local build. No Science publisher, owned route or game source is edited.
