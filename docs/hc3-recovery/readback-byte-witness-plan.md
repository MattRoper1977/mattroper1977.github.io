# HC3 readback byte-witness repair

The dated HC3 readback currently accepts Apps/Lessons exact runner SHA plus a successful live-proof job label. Actual call-site review and artifacts show that this older browser gate uses a fixed marker; it cannot bind a publication. Require parsed, digest-verified source-bound byte evidence before any served-proof count. Preserve deployment status as a separate observation and keep whole-deck/link/handoff counts unknown. Recover the original small FieldOps archive10002185828 and its attempt2 metadata as the real dated fixture; derive group-specific membership from its44 original rows. Controls: real witness passes, removed witness with green jobs rejects, wrong SHA/publication/attempt/archive/duplicate row rejects, restored passes. No pupil content or publisher output changes. Rollback before implementation: Site493904ebd6814a447e5239b85e4e25be5b3a9a8b.
