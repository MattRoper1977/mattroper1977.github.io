# PASS U — PLAN

## Letter / branch / repos
- **Pass U** (letter Q spent on the site repo). No existing "U" ledger found → letter kept.
- Branch **`pass-u-audit`** off origin/main (04a40b4) in the **site repo** (mattroper1977.github.io) — this is
  where the sitemap sub-pass (§4) lives; the sitemap of record is here.
- The §5 Lessons-estate sweep runs against the Lessons clone (/workspace/lessons @ 32ca685e, unshallowed)
  and needs its OWN `pass-u-audit` branch + push access in MattRoper1977/Lessons (this session can't push there).
- Working dir `_passu/` — COMMITTED to the branch (not git-excluded).

## Pass Q state (established, not assumed)
- Pass Q tip 6845f44 on `claude/pass-q-audit-c5tg3s` is **NOT merged**; origin/main advanced to 04a40b4.
- ⇒ deployment still serves type "Teacher tool"; the union-table rename is PROPOSED, not live. Any finding
  depending on it is conditional. The three post-merge browser checks are still the reviewer's to run.

## Work items
1. §2 · reconcile Pass Q's 9-vs-7 scripted-page count at 6845f44. [DONE]
2. §4 · whole-domain sitemap: candidate model → policy gate (reviewer per-folder calls) → generate (hand-back)
   → verify. NO cross-repo push; NO write to live sitemap.xml (reviewer applies). [DONE — handed back]
3. §5 · Lessons estate sweep (original brief): Phase 0 [DONE] → carry-forward reconciliation → sweep →
   sitemap gate (done) → fixes by tier → close-out. [ONGOING — needs Lessons branch + push]

## Carry-forwards honoured
- Taxonomy union table: checked Lessons type values — no new synonym pair (§5 Phase 0).
- Chip-guard obligation: any type change proposed here waits for _passq/chips_check.py green (from the Pass Q branch).
- Sitemap drift set read from the committed carry-forwards (0 dead), NOT from any prose figure.
- Do-not-fix ledger re-read; scheduled programmes (heavy-tier RM chassis, 500ms nudge, reading-theme port)
  stay out — rediscovery logs as "known, scheduled", not a commit.

## Discipline
Decode before diffing a sitemap against a tree; ground-truth every count against a real listing before it
enters the ledger (the Pass Q "70 dead" error). Specimen-per-chassis for §5. Assessed lessons: alone, one hunk.
